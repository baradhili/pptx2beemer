# Changelog — cascade fork, `master`

The fork tracks upstream transpect/cascade master; no fork-specific commits yet.
The docx2tex `further-fixes` pass advanced the pin by three upstream commits
(content base URI support, all parsed params ending up in the param set by
default, LICENSE update).
