# Changelog — evolve-hub fork, `master`

The fork tracks upstream transpect/evolve-hub master; no fork-specific commits yet.
The docx2tex `further-fixes` pass advanced the pin by five upstream commits
(list-marker detection inside direct formatting, identifier cleanup via same-scope
functions, override-attr xpath fixes, and a colwidth guard that ignores columns
less than 1pi/pt wide).
