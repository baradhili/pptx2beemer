# Changelog — calabash-frontend fork, `master`

The fork tracks upstream transpect/calabash-frontend master; no fork-specific
commits yet. The docx2tex `further-fixes` pass advanced the pin by one upstream
commit (Java version evaluation fix for modern and legacy formats).
