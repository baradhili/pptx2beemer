<?xml version="1.0" encoding="UTF-8"?>
<?xml-model href="http://transpect.io/xml2tex/schema/xml2tex.rng" type="application/xml" schematypens="http://relaxng.org/ns/structure/1.0"?>
<?xml-model href="http://transpect.io/xml2tex/schema/xml2tex.rng" type="application/xml" schematypens="http://purl.oclc.org/dsdl/schematron"?>
<set xmlns="http://transpect.io/xml2tex"  
     xmlns:tr="http://transpect.io"
     xmlns:xsl="http://www.w3.org/1999/XSL/Transform">
  
  <import href="conf.charmap.xml"/>

  <xsl:import href="http://transpect.io/mml2tex/xsl/mml2tex.xsl"/>
  <xsl:import href="http://transpect.io/xslt-util/colors/xsl/colors.xsl"/>
  <xsl:import href="http://transpect.io/xslt-util/paths/xsl/paths.xsl"/>
  <xsl:import href="http://transpect.io/xslt-util/roman-numerals/xsl/roman2int.xsl"/>
  
  <xsl:param name="table-model" as="xs:string"/>
  <xsl:param name="a11y" as="xs:boolean" select="false()">
    <!-- if true() add paragraph structures -->
  </xsl:param>
  
  <xsl:variable name="xerif" as="xs:boolean" select="false()"/>
  <xsl:variable name="root" as="document-node()" select="root()"/>
  <xsl:variable name="path"       as="xs:string"  select="tr:path(base-uri())"/>
  <xsl:variable name="basename"   as="xs:string"  select="tr:basename(/dbk:hub/@xml:base)"/>
  <xsl:variable name="colors"     as="xs:string*"
                select="(
                         //@css:background-color,
                         //@css:color[not(every $i in  tr:hex-rgb-color-to-ints(.) satisfies $i &lt; 35
                                          or parent::*[(@role, @name) = ('ZFinlineequation', 'ZFequation')]
                                          or ancestor::dbk:para/dbk:phrase[@role = ('ZFinlineequation', 'ZFequation', 'Hyperlink')]
                                          or ancestor-or-self::dbk:link
                                          or parent::*[@native-name eq 'Hyperlink'])],
                         //@css:border-top-color[starts-with(., '#')],
                         //@css:border-bottom-color[starts-with(., '#')],
                         //@css:border-left-color[starts-with(., '#')],
                         //@css:border-right-color[starts-with(., '#')],
                         for $mml2tex-color in (
                                        for $mml2tex-snippet in //processing-instruction('mml2tex')/tokenize(., '\\textcolor') 
                                        return replace($mml2tex-snippet, '\{([-a-z0-9]+?)\}.*$', '$1', 'i')
                                        )[not(position() eq 1)]
                         return if(starts-with($mml2tex-color, '#')) then $mml2tex-color else tr:color-keyword-to-hex-rgb($mml2tex-color)
                         )"/>
  <xsl:variable name="langs" select="distinct-values(//@xml:lang)" as="xs:string*"/>
  
  <xsl:variable name="footnotes" select="//dbk:footnote" as="element(dbk:footnote)*"/>

  <!-- bottom band occupied by the Word footer (vision text baseline 121pt above the page
       bottom, see the \put(56.7,121) below, plus one 10pt line ascent and clearance),
       drawn as an absolute shipout picture; body text must be kept out of it -->
  <xsl:variable name="footer-band-height" as="xs:double?"
                select="if (exists((//dbk:div[@role eq 'docx2hub:footer'][@condition eq 'default'])[1]))
                        then 121 + 12
                        else ()"/>

  <!-- shrink \textheight on the pages before the first section break (the cover) so
       body text stays above the footer band; \newgeometry is not used because it also
       resets topskip/headheight etc. to package defaults and shifts the text block -->
  <xsl:variable name="cover-footer-reserve" as="xs:string?"
                select="if ($footer-band-height
                            and /dbk:hub/@css:page-width
                            and number(replace(/dbk:hub/@css:margin-bottom, '[a-z]+$', '')) &lt; $footer-band-height)
                        then concat('-', $footer-band-height - number(replace(/dbk:hub/@css:margin-bottom, '[a-z]+$', '')), 'bp')
                        else ()"/>
  
  <xsl:variable name="letter-spacing-def" select="'soul'" as="xs:string?">
    <!-- define which makro is used for letter spacing. allowed values: 
    - 'soul' creates \so and includes package soul 
    - 'microtype' creates \textls and includes package microtype 
    - '' creates \textls and includes no package (for overriding) -->
  </xsl:variable>
  
  <!-- elements where page breaks are ignored -->
  
  <xsl:variable name="elements-where-pagebreaks-are-ignored" as="xs:string+" 
                select="'part', 'chapter', 'section', 'index'"/>
  <xsl:variable name="titles-where-pagebreaks-are-ignored" as="xs:string+" 
                select="'part', 'chapter', 'section', 'index'"/>
  
  <xsl:key name="style" match="css:rule" use="@name"/>
  <xsl:key name="item-by-id" match="*[@xml:id]" use="@xml:id"/>
  
  <xsl:function name="tr:add-cca" as="xs:string?">
    <xsl:param name="pos" as="xs:string"><!-- 'Start' or 'End'--></xsl:param>
    <xsl:param name="type" as="xs:string"/><!-- P or Artifact-->
    <xsl:sequence select="if ($a11y) then  concat('\ccaVstruct', $pos, '{', $type, '}')  else ()"/>
  </xsl:function>

  <!-- VML shape styles carry CSS units and Word writes any of them
       (e.g. 'margin-top:5in'); a bare number means points. Convert per unit;
       values that are not a plain length yield the empty sequence. -->
  <xsl:function name="tr:css-length-to-pt" as="xs:double?">
    <xsl:param name="length" as="xs:string?"/>
    <xsl:analyze-string select="normalize-space($length)" regex="^([+-]?[0-9.]+)\s*(pt|in|cm|mm|pc|px)?$">
      <xsl:matching-substring>
        <xsl:sequence select="xs:double(regex-group(1))
                              * (if (regex-group(2) eq 'in') then 72e0
                                 else if (regex-group(2) eq 'cm') then 72e0 div 2.54e0
                                 else if (regex-group(2) eq 'mm') then 72e0 div 25.4e0
                                 else if (regex-group(2) eq 'pc') then 12e0
                                 else if (regex-group(2) eq 'px') then 0.75e0
                                 else 1e0)"/>
      </xsl:matching-substring>
    </xsl:analyze-string>
  </xsl:function>
  
  <!-- tex document header, set declarations such as documentclass and usepackage -->
  <preamble>% pptx2beemer --- PPTX to LaTeX beamer, based on docx2tex/xml2tex
    % https://github.com/baradhili/pptx2beemer
    %
    \documentclass{beamer}
    \usepackage{iftex}
    \usepackage{amsmath}
    \usepackage{amssymb}
    \usepackage{amsfonts}
    \usepackage{amsxtra}
    \usepackage{wasysym}
    \usepackage{isomath}
    \usepackage{mathtools}
    % txfonts must load after amsmath (its \iint etc. collide with amsmath's
    % otherwise) and before fontspec/tgheros so the docx fonts win for text
    \usepackage{txfonts}
    \ifPDFTeX
      \usepackage[T1]{fontenc}
      \usepackage[utf8]{inputenc}
      \usepackage{tgheros}
      \renewcommand{\familydefault}{\sfdefault}
    \else
      % LuaLaTeX/XeLaTeX: use the real fonts from the docx, no substitution
      \usepackage{fontspec}
      \setmainfont{Arial}
      \setsansfont{Arial}
    \fi
    \usepackage{anyfontsize}
    <xsl:if test="//dbk:imagedata">
      <xsl:text>\usepackage{graphicx}&#xa;</xsl:text>
      <!-- savebox for srcRect-cropped images; dedicated and without @ because the
           crop code is used in the document body where @ is not a letter -->
      <xsl:if test="//dbk:imagedata[@css:crop-top or @css:crop-right or @css:crop-bottom or @css:crop-left]">
        <xsl:text>\newsavebox{\docxcroppedimagebox}&#xa;</xsl:text>
      </xsl:if>
    </xsl:if>
    <xsl:if test="/dbk:hub/@css:page-width">
      <xsl:text>\usepackage{geometry}&#xa;</xsl:text>
      <!-- docx lengths (twips/20) are PDF points (bp); TeX pt are 0.37 % smaller, so emit
           bp units. Use a named standard size when the dimensions match one (portrait),
           numeric bp otherwise -->
      <xsl:variable name="page-width-bp" as="xs:double" select="number(replace(/dbk:hub/@css:page-width, '[a-z]+$', ''))"/>
      <xsl:variable name="page-height-bp" as="xs:double" select="number(replace(/dbk:hub/@css:page-height, '[a-z]+$', ''))"/>
      <xsl:variable name="paper-option" as="xs:string?">
        <xsl:choose>
          <xsl:when test="abs($page-width-bp - 595.3) lt 0.5 and abs($page-height-bp - 841.9) lt 0.5">a4paper</xsl:when>
          <xsl:when test="abs($page-width-bp - 419.5) lt 0.5 and abs($page-height-bp - 595.3) lt 0.5">a5paper</xsl:when>
          <xsl:when test="abs($page-width-bp - 841.9) lt 0.5 and abs($page-height-bp - 1190.55) lt 0.5">a3paper</xsl:when>
          <xsl:when test="abs($page-width-bp - 612) lt 0.5 and abs($page-height-bp - 792) lt 0.5">letterpaper</xsl:when>
          <xsl:when test="abs($page-width-bp - 612) lt 0.5 and abs($page-height-bp - 1008) lt 0.5">legalpaper</xsl:when>
          <xsl:when test="abs($page-width-bp - 522) lt 0.5 and abs($page-height-bp - 756) lt 0.5">executivepaper</xsl:when>
        </xsl:choose>
      </xsl:variable>
      <xsl:value-of select="concat('\geometry{',
                                   if ($paper-option)
                                   then $paper-option
                                   else concat('paperwidth=', $page-width-bp, 'bp, paperheight=', $page-height-bp, 'bp'),
                                   ', top=', replace(/dbk:hub/@css:margin-top, '[a-z]+$', ''),
                                   'bp, bottom=', replace(/dbk:hub/@css:margin-bottom, '[a-z]+$', ''),
                                   'bp, left=', replace(/dbk:hub/@css:margin-left, '[a-z]+$', ''),
                                   'bp, right=', replace(/dbk:hub/@css:margin-right, '[a-z]+$', ''),
                                   'bp}&#xa;')"/>
    </xsl:if>
    \usepackage{hyperref}
    \usepackage{multirow}
    \usepackage{tabularx}
    \usepackage{color}
    <xsl:if test="//*[@css:background-color]">
      <xsl:text>\usepackage{colortbl}&#xa;</xsl:text>
    </xsl:if>
    \usepackage{textcomp}
    <xsl:choose>
      <xsl:when test="//dbk:div[@role eq 'hub:index'][1]">
        <xsl:text>\usepackage[safe]{tipa}&#xa;</xsl:text>
      </xsl:when>
      <xsl:otherwise>
        <xsl:text>\usepackage{tipa}&#xa;</xsl:text>
      </xsl:otherwise>
    </xsl:choose>
    \usepackage{upgreek}
    \usepackage{enumerate}
    \usepackage{tensor}
    \usepackage{pifont}
    \usepackage{ulem}
    \usepackage{xfrac}
    <xsl:if test="//*[@css:letter-spacing or exists(key('style', @role)/@css:letter-spacing)] and $letter-spacing-def[normalize-space()]">
      <xsl:value-of select="concat('\usepackage{', $letter-spacing-def,'}&#xa;')"/>
      <xsl:if test="$letter-spacing-def eq 'soul'">
        <!-- soul analyses its argument in a hardcoded 8-bit ec typewriter font
             (ectt1000); glyphs it lacks (e.g. the bullet U+2022) measure zero
             and vanish from the \so output. Under LuaLaTeX switch the analysis
             font to a Unicode one and re-measure soul's hyphen width from it,
             keeping the syllable arithmetic consistent. -->
        <xsl:text>\makeatletter\ifPDFTeX\else&#xa;</xsl:text>
        <xsl:text>\font\SOUL@tt=[lmmono10-regular.otf]\relax&#xa;</xsl:text>
        <xsl:text>\setbox\z@\hbox{\SOUL@tt-}\SOUL@ttwidth\wd\z@&#xa;</xsl:text>
        <xsl:text>\fi\makeatother&#xa;</xsl:text>
        <!-- one letterspacing command per distinct Word tracking value: soul's
             default \so spaces at .25em (2.1pt at 8.5pt) where Word declares
             absolute points (0.85–0.9pt here) — the wider tracking made lines
             wrap well before Word. Letterskip = the declared pt value, the
             space = the font's natural interword space plus the tracking
             (evaluated at use time, so it scales with the current font). -->
        <xsl:for-each select="$docx-letterspacings[position() le 26]">
          <xsl:text>\sodef\docxso</xsl:text>
          <xsl:value-of select="substring('ABCDEFGHIJKLMNOPQRSTUVWXYZ', position(), 1)"/>
          <xsl:text>{}{</xsl:text>
          <xsl:value-of select="."/>
          <xsl:text>pt}{\dimexpr\fontdimen2\font+</xsl:text>
          <xsl:value-of select="."/>
          <xsl:text>pt\relax}{</xsl:text>
          <xsl:value-of select="."/>
          <xsl:text>pt}&#xa;</xsl:text>
        </xsl:for-each>
      </xsl:if>
    </xsl:if>
    \usepackage{arydshln}
    <!-- paragraph spacing = the Normal style's space-after (Word default 0 when
         not declared), surfaced on the hub root by docx2hub -->
    <xsl:variable name="default-parskip" as="xs:string"
                  select="replace((/dbk:hub/@css:default-space-after, '0pt')[1], '[a-z]+$', '')"/>
    <xsl:text>\setlength{\parskip}{</xsl:text>
    <xsl:value-of select="$default-parskip"/>
    <xsl:text>pt}&#xa;\setlength{\parindent}{0pt}&#xa;</xsl:text>
    <!-- absolutely positioned VML text boxes (Word floating shapes) render via
         textpos at their Word coordinates; modules of 1pt make the textblock
         arguments plain points from the paper's top-left corner -->
    <xsl:if test="exists(//w:pict[v:shape/v:textbox][contains(v:shape/@style, 'position:absolute')][contains(v:shape/@style, 'margin-left')][contains(v:shape/@style, 'margin-top')])
                  or exists(//dbk:imagedata[@css:position-left][@css:position-top]
                                       [not(ancestor::dbk:div[@role = ('docx2hub:header', 'docx2hub:footer')])])">
      <xsl:text>\usepackage[absolute]{textpos}&#xa;</xsl:text>
      <xsl:text>\setlength{\TPHorizModule}{1pt}\setlength{\TPVertModule}{1pt}\textblockorigin{0pt}{0pt}&#xa;</xsl:text>
    </xsl:if>

    <!-- a "1.5 lines" (or other) Normal style spaces the whole document (Word
         default line spacing, surfaced on the hub root by docx2hub) -->
    <xsl:if test="/dbk:hub/@css:default-line-height[. castable as xs:double][xs:double(.) gt 1]">
      <xsl:text>\linespread{</xsl:text>
      <xsl:value-of select="/dbk:hub/@css:default-line-height"/>
      <xsl:text>}&#xa;</xsl:text>
    </xsl:if>
    <!-- match the LaTeX sectioning commands to the docx Heading{n} styles: one
         titlesec \titleformat per level, generated from the styles' css:rule
         (font size, weight, colour). Chapters become 'straight' (inline number,
         no page break), like Word headings; the vertical spacing stays with the
         paragraphs' own Word margins. -->
    <xsl:if test="exists(//css:rule[matches(@remap, '^h[1-6]$')])">
      <xsl:text>\usepackage{titlesec}&#xa;</xsl:text>
      <xsl:text>\titleclass{\chapter}{straight}&#xa;</xsl:text>
      <xsl:for-each select="(for $l in 1 to 6
                               return (//css:rule[@name eq concat('Heading', $l)],
                                       //css:rule[@remap eq concat('h', $l)])[1])">
        <xsl:variable name="level" select="xs:integer(substring(@remap, 2))" as="xs:integer"/>
        <xsl:variable name="cmd" select="('chapter', 'section', 'subsection', 'subsubsection', 'paragraph', 'subparagraph')[$level]" as="xs:string"/>
        <xsl:variable name="size" select="number(replace(@css:font-size, '[a-z]+$', ''))[. gt 0]" as="xs:double?"/>
        <xsl:text>\titleformat{\</xsl:text>
        <xsl:value-of select="$cmd"/>
        <xsl:text>}{</xsl:text>
        <xsl:if test="exists($size)">
          <xsl:text>\fontsize{</xsl:text>
          <xsl:value-of select="$size"/>
          <xsl:text>pt}{</xsl:text>
          <xsl:value-of select="round($size * 1.15 * 10) div 10"/>
          <xsl:text>pt}\selectfont</xsl:text>
        </xsl:if>
        <xsl:if test="@css:font-weight eq 'bold'">
          <xsl:text>\bfseries</xsl:text>
        </xsl:if>
        <xsl:if test="@css:color">
          <xsl:variable name="heading-color" select="@css:color" as="xs:string"/>
          <xsl:text>\color{</xsl:text>
          <xsl:value-of select="if (exists(tr:color-hex-rgb-to-keyword($heading-color)))
                                then tr:color-hex-rgb-to-keyword($heading-color)[1]
                                else concat('color-', upper-case(substring-after($heading-color, '#')))"/>
          <xsl:text>}</xsl:text>
        </xsl:if>
        <xsl:text>}{\the</xsl:text>
        <xsl:value-of select="$cmd"/>
        <xsl:if test="$level eq 1">
          <xsl:text>.</xsl:text>
        </xsl:if>
        <xsl:text>}{0.6em}{}&#xa;</xsl:text>
        <xsl:text>\titlespacing*{\</xsl:text>
        <xsl:value-of select="$cmd"/>
        <xsl:text>}{0pt}{0pt}{0pt}&#xa;</xsl:text>
      </xsl:for-each>
    </xsl:if>
    <!-- documents whose Word headings carry no list numbering must not get LaTeX
         numbers either: when no headline paragraph has a Word number identifier,
         disable section numbering (titlesec then drops the labels) -->
    <xsl:if test="exists(//dbk:para[@docx2tex:config eq 'headline'])
                  and empty(//dbk:para[@docx2tex:config eq 'headline']
                                  [processing-instruction('latex')[starts-with(., '\label{mark-')]])">
      <xsl:text>\setcounter{secnumdepth}{-2}&#xa;</xsl:text>
    </xsl:if>
    <!-- Word headers/footers (included by docx2hub with include-header-and-footer=yes):
         every section's default header/footer part becomes a fancyhdr page style,
         switched at the Word section breaks (see the section-break template below).
         Absolutely positioned elements (classification marking, cover artwork,
         full-bleed bands) are rendered via eso-pic instead. -->
    <xsl:variable name="docx-headers" select="//dbk:div[@role eq 'docx2hub:header'][@condition eq 'default']" as="element(dbk:div)*"/>
    <xsl:variable name="docx-footers" select="//dbk:div[@role eq 'docx2hub:footer'][@condition eq 'default']" as="element(dbk:div)*"/>
    <!-- resolve the document's phrase fonts (headers, footers and body runs)
       once, in the preamble: a missing font must not trigger a luaotfload
       reload from inside the output routine. Must not depend on the presence
       of header/footer parts (documents without any reference the macros too) -->
    <xsl:for-each select="$docx-hf-fonts">
      <xsl:text>\ifPDFTeX\else\IfFontExistsTF{</xsl:text>
      <xsl:value-of select="."/>
      <xsl:text>}{\newfontfamily\docxhffont</xsl:text>
      <xsl:value-of select="substring('ABCDEFGHIJKLMNOPQRSTUVWXYZ', position(), 1)"/>
      <xsl:text>{</xsl:text>
      <xsl:value-of select="."/>
      <xsl:text>}}{\newcommand{\docxhffont</xsl:text>
      <xsl:value-of select="substring('ABCDEFGHIJKLMNOPQRSTUVWXYZ', position(), 1)"/>
      <xsl:text>}{}\typeout{docx2tex WARNING: font '</xsl:text>
      <xsl:value-of select="."/>
      <xsl:text>' not found - install it and rerun; using the fallback document font}}\fi&#xa;</xsl:text>
    </xsl:for-each>

    <xsl:if test="exists($docx-headers)">
      <xsl:text>\usepackage{eso-pic}&#xa;\usepackage{fancyhdr}&#xa;\usepackage{zref-abspage}&#xa;</xsl:text>
      <!-- total number of physical pages, independent of numbering restarts
           (zref-abspage writes it to the aux as \@abspage@last) -->
      <xsl:text>\makeatletter\providecommand\@abspage@last{1}\newcommand{\docxpagecount}{\@abspage@last}\makeatother&#xa;</xsl:text>
      <!-- Word starts a new section's content on the next page whatever its parity;
           without this, scrbook's twoside \chapter inserts blank verso pages -->
      <xsl:text>\KOMAoption{open}{any}&#xa;</xsl:text>
      <!-- place the header baseline near Word's header line (measured: rule at 62pt
           below the page top) and the footer text above the page bottom as in Word -->
      <xsl:text>\geometry{headheight=14pt,headsep=36.9pt,footskip=20pt}&#xa;</xsl:text>
      <!-- Word's header paragraph is tall (it anchors the marking text box), so its
           bottom border (the rule) sits well below the header text -->
      <xsl:text>\renewcommand{\headruleskip}{14.9pt}&#xa;</xsl:text>
      <!-- the classification marking (e.g. red OFFICIAL, page-anchored text box) is
           shared by every section's header part: keep it as a macro so it can be
           re-added after the shipout stack is cleared for a later section -->
      <xsl:text>\newcommand{\docxmarking}{%&#xa;</xsl:text>
      <xsl:variable name="hdr" select="$docx-headers[1]" as="element(dbk:div)"/>
      <!-- marking text: Word anchors the classification-marking textbox at the
           page top, centered (mso-position-horizontal-relative:page); the
           reference renders the 10pt Calibri OFFICIAL baseline 24.5pt below the
           page top. The \put reference point itself renders ~3pt above the text
           baseline, so the put goes 21.5pt below the page top. -->
      <xsl:variable name="marking-page-centered" as="xs:boolean"
                    select="exists($hdr//dbk:phrase[@role eq 'hub:foreign']//@style
                                     [contains(., 'mso-position-horizontal-relative:page')]
                                     [contains(., 'mso-position-horizontal:center')])"/>
      <xsl:choose>
        <xsl:when test="$marking-page-centered">
          <xsl:text>  \put(\LenToUnit{0.5\paperwidth},</xsl:text>
          <xsl:value-of select="number(replace(/dbk:hub/@css:page-height, '[a-z]+$', '')) - 21.5"/>
          <xsl:text>){\makebox[0pt][c]{%&#xa;</xsl:text>
        </xsl:when>
        <xsl:otherwise>
          <xsl:text>  \put(56.7,</xsl:text>
          <xsl:value-of select="number(replace(/dbk:hub/@css:page-height, '[a-z]+$', '')) - 21.5"/>
          <xsl:text>){%&#xa;</xsl:text>
        </xsl:otherwise>
      </xsl:choose>
      <xsl:apply-templates select="$hdr/node()" mode="#current"/>
      <xsl:if test="$marking-page-centered">
        <xsl:text>}&#xa;</xsl:text>
      </xsl:if>
      <xsl:text>}&#xa;</xsl:text>
      <xsl:text>}&#xa;</xsl:text>
      <xsl:text>\AddToShipoutPicture{\docxmarking}&#xa;</xsl:text>
      <!-- cover section (section 1): logo in the header, vision text and banner in
           the footer, at absolute positions matching the Word anchoring -->
      <xsl:text>\AddToShipoutPicture{%&#xa;</xsl:text>
      <xsl:apply-templates select="$docx-headers[1]//dbk:imagedata[@fileref][@css:width][ancestor::dbk:div[@role eq 'docx2hub:header']]" mode="#current"/>
      <xsl:apply-templates select="$docx-headers[1]//dbk:imagedata[svg:svg][ancestor::dbk:div[@role eq 'docx2hub:header']]" mode="#current"/>
      <xsl:variable name="cover-ftr" select="$docx-footers[1]" as="element(dbk:div)?"/>
      <xsl:if test="$cover-ftr">
        <xsl:if test="$cover-ftr//dbk:phrase[normalize-space(.)] or $cover-ftr/text()[normalize-space(.)]">
          <!-- vision text: left margin, baseline 121pt above the page bottom -->
          <xsl:text>  \put(56.7,121){%&#xa;</xsl:text>
          <xsl:apply-templates select="$cover-ftr/node()" mode="#current"/>
          <xsl:text>}&#xa;</xsl:text>
        </xsl:if>
        <xsl:apply-templates select="$cover-ftr//dbk:imagedata[@fileref][@css:width][ancestor::dbk:div[@role eq 'docx2hub:footer']]" mode="#current"/>
      </xsl:if>
      <xsl:text>}&#xa;</xsl:text>
      <!-- sections 2..n: one fancyhdr page style per section, built from that
           section's default header/footer parts -->
      <xsl:for-each select="$docx-headers[position() ge 2]">
        <xsl:variable name="section" select="position() + 1" as="xs:integer"/>
        <xsl:variable name="hdr" select="." as="element(dbk:div)"/>
        <xsl:variable name="ftr" select="$docx-footers[$section]" as="element(dbk:div)?"/>
        <xsl:variable name="footer-entries" as="element(dbk:entry)*"
                      select="$ftr//dbk:entry[dbk:para[normalize-space(string(.))]]"/>
        <xsl:variable name="footer-paras" as="element(dbk:para)*"
                      select="$ftr/dbk:para[normalize-space(string(.))]"/>
        <xsl:text>\fancypagestyle{docx-section-</xsl:text>
        <xsl:value-of select="$section"/>
        <xsl:text>}{%&#xa;  \fancyhf{}%&#xa;</xsl:text>
        <!-- header: the section's header text (marking text box and images are
             rendered separately, at absolute positions) -->
        <xsl:for-each select="$hdr/dbk:para[normalize-space(string(.))]
                                    [exists(node()[not(self::dbk:tabs)][not(self::dbk:phrase[@role eq 'hub:foreign'])][not(self::dbk:phrase[dbk:phrase[@role eq 'hub:foreign']][not(text()[normalize-space()])])][normalize-space(string(.))])]">
          <xsl:call-template name="docx2tex-hf-entry">
            <xsl:with-param name="kind" select="'head'"/>
          </xsl:call-template>
        </xsl:for-each>
        <!-- footer: Word layouts the footer as a borderless table; first text cell
             left, last text cell right -->
        <xsl:for-each select="$footer-entries">
          <xsl:if test="position() eq 1 or (position() eq last() and position() gt 1)">
            <xsl:variable name="entry-side" select="if (position() eq 1) then 'L' else 'R'" as="xs:string"/>
            <xsl:for-each select="dbk:para[1]">
              <xsl:call-template name="docx2tex-hf-entry">
                <xsl:with-param name="kind" select="'foot'"/>
                <xsl:with-param name="side" select="$entry-side"/>
              </xsl:call-template>
            </xsl:for-each>
          </xsl:if>
        </xsl:for-each>
        <xsl:for-each select="$footer-paras">
          <xsl:call-template name="docx2tex-hf-entry">
            <xsl:with-param name="kind" select="'foot'"/>
          </xsl:call-template>
        </xsl:for-each>
        <!-- rules: Word draws the header para's bottom border and the footer cells'
             top border in the section's rule color -->
        <xsl:variable name="head-rule-width" select="replace(($hdr/dbk:para/@css:border-bottom-width)[1], '[a-z]+$', '')" as="xs:string?"/>
        <xsl:choose>
          <xsl:when test="normalize-space($head-rule-width)">
            <xsl:text>  \renewcommand{\headrulewidth}{</xsl:text>
            <xsl:value-of select="$head-rule-width"/>
            <xsl:text>pt}&#xa;</xsl:text>
          </xsl:when>
          <xsl:otherwise>
            <xsl:text>  \renewcommand{\headrulewidth}{0pt}&#xa;</xsl:text>
          </xsl:otherwise>
        </xsl:choose>
        <xsl:variable name="foot-rule-width" select="replace(($footer-entries/@css:border-top-width)[1], '[a-z]+$', '')" as="xs:string?"/>
        <xsl:choose>
          <xsl:when test="normalize-space($foot-rule-width)">
            <xsl:text>  \renewcommand{\footrulewidth}{</xsl:text>
            <xsl:value-of select="$foot-rule-width"/>
            <xsl:text>pt}&#xa;</xsl:text>
          </xsl:when>
          <xsl:otherwise>
            <xsl:text>  \renewcommand{\footrulewidth}{0pt}&#xa;</xsl:text>
          </xsl:otherwise>
        </xsl:choose>
        <xsl:text>}&#xa;</xsl:text>
      </xsl:for-each>
    </xsl:if>
    <!-- beamer draws its own headlines/footlines; the docx fancyhdr machinery
         does not apply to presentations -->
    <xsl:sequence select="if(exists($langs))
                          then string-join(xml2tex:langs-to-latex-pkg( $langs ), '&#xa;') 
                          else string-join(xml2tex:langs-to-latex-pkg( 'en' ), '&#xa;')"/>
    <xsl:text>&#xa;</xsl:text>
    <xsl:for-each select="xml2tex:rgb-to-tex-color( $colors )">
      <xsl:value-of select="concat('\define', ., '&#xa;')"/>
    </xsl:for-each>
  </preamble>

  <!-- xml namespace declaration -->

  <ns prefix="dbk" uri="http://docbook.org/ns/docbook"/>
  <ns prefix="html" uri="http://www.w3.org/1999/xhtml"/>
  <ns prefix="css" uri="http://www.w3.org/1996/css"/>
  <ns prefix="svg" uri="http://www.w3.org/2000/svg"/>
  <ns prefix="docx2tex" uri="http://transpect.io/docx2tex"/>
  <ns prefix="docx2hub" uri="http://transpect.io/docx2hub"/>
  <ns prefix="w" uri="http://schemas.openxmlformats.org/wordprocessingml/2006/main"/>
  <ns prefix="v" uri="urn:schemas-microsoft-com:vml"/>
  <ns prefix="mml2tex" uri="http://transpect.io/mml2tex"/>
  <ns prefix="tr" uri="http://transpect.io"/>

  <!-- one Word header/footer paragraph as a fancyhdr entry: side from the para's
       text-align (or the footer-table cell position), in the paragraph style's
       font size and weight (Word footer styles are often 9 pt bold) -->
  <xsl:template name="docx2tex-hf-entry">
    <xsl:param name="kind" as="xs:string" select="'head'"/>
    <xsl:param name="side" as="xs:string"
               select="if (@css:text-align eq 'right') then 'R'
                       else if (@css:text-align eq 'center') then 'C'
                       else 'L'"/>
    <xsl:variable name="hf-size" as="xs:double?"
                  select="max(for $sz in (if (exists(.//dbk:phrase/@css:font-size))
                                           then .//dbk:phrase/@css:font-size
                                           else (@css:font-size, key('style', @role)/@css:font-size))
                                       return number(replace($sz, '[a-z]+$', '')))"/>
    <xsl:variable name="hf-bold" as="xs:boolean"
                  select="string((@css:font-weight, key('style', @role)/@css:font-weight)[1]) eq 'bold'"/>
    <xsl:text>  \fancy</xsl:text>
    <xsl:value-of select="$kind"/>
    <xsl:text>[</xsl:text>
    <xsl:value-of select="$side"/>
    <xsl:text>]{</xsl:text>
    <xsl:if test="exists($hf-size) and $hf-size ne 12">
      <xsl:text>{\fontsize{</xsl:text>
      <xsl:value-of select="$hf-size"/>
      <xsl:text>pt}{</xsl:text>
      <xsl:value-of select="round($hf-size * 1.15 * 10) div 10"/>
      <xsl:text>pt}\selectfont </xsl:text>
    </xsl:if>
    <xsl:if test="$hf-bold">
      <xsl:text>\bfseries </xsl:text>
    </xsl:if>
    <xsl:apply-templates select="node()[not(self::dbk:tabs)][not(self::dbk:phrase[@role eq 'hub:foreign'])][not(self::dbk:phrase[dbk:phrase[@role eq 'hub:foreign']][not(text()[normalize-space()])])]" mode="#current"/>
    <xsl:if test="exists($hf-size) and $hf-size ne 12">
      <xsl:text>}</xsl:text>
    </xsl:if>
    <xsl:text>}%&#xa;</xsl:text>
  </xsl:template>

  <!-- drop metadata -->

  <template context="/dbk:hub/dbk:info"/>
  
  <!-- regular paras -->

  <template context="dbk:para[not(parent::*[local-name() = ('entry', 'th', 'td')])]
                             [following-sibling::*[1][not(local-name() = ('orderedlist', 
                                                                          'itemizedlist', 
                                                                          'variablelist', 
                                                                          'figure', 
                                                                          'equation', 
                                                                          'dialogue', 
                                                                          'blockquote'))]]
                             [not(parent::dbk:listitem) or (parent::dbk:listitem and following-sibling::*[1][self::dbk:para])]">
    <xsl:param name="suppress-cca" select="tr:suppress-structure(.)" tunnel="yes" as="xs:boolean?"/>
    <xsl:param name="already-structured" tunnel="yes" as="xs:boolean?"/>
    <rule break-after="2">
      <text select="tr:add-cca('Start', 'P')[not($suppress-cca) and not($already-structured)]"/>
      <text/>
      <text select="tr:add-cca('End', 'P')[not($suppress-cca) and not($already-structured)]"/>
    </rule>
  </template>
  
  <xsl:function name="tr:suppress-structure" as="xs:boolean">
    <xsl:param name="elt" as="element()"/>
    <!-- add conditions to not generate structure. if outcome is true: no structure will be added.  -->
    <xsl:sequence select="if ($a11y) 
                          then exists($elt[parent::*[self::*:legalnotice] 
                                           or 
                                           (every $node in node() satisfies $node[self::processing-instruction()])
                                          ]
                                     ) 
                          else true()"/>
  </xsl:function>
  
  <template context="*[not(local-name() = $elements-where-pagebreaks-are-ignored)]
                      [not(self::dbk:title and parent::dbk:info/parent::*/local-name() = $titles-where-pagebreaks-are-ignored)]
                      [@css:page-break-before eq 'always' or key('style', @role)/@css:page-break-before eq 'always']">
    <xsl:if test="$xerif"><xsl:text>\pagebreak&#xa;</xsl:text></xsl:if>
    <xsl:next-match/>
  </template>  
  
  <!-- carriage returns -->
  
  <template context="dbk:phrase[@role eq 'cr']">
    <rule name="newline" type="cmd" break-after="1"/>
  </template>
  
  <!-- line breaks -->
  
  <template context="dbk:br">
    <rule name="newline" type="cmd" break-after="1"/>
  </template>
  
  <template context="dbk:br[following-sibling::node()[1][self::dbk:br]]">
    <rule name="newline" type="cmd"/>
  </template>

  <template context="dbk:title/dbk:br">
    <rule name="protect\newline" type="cmd" break-after="1"/>
  </template>
  
  <template context="dbk:entry//dbk:br[not($xerif)]">
    <rule>
      <text> \newline </text>
    </rule>
  </template>
  
  <template context="html:td//dbk:br[$table-model eq 'htmltabs']">
    <rule>
      <text> \htCellBreak </text>
    </rule>
  </template>
  
  <template context="dbk:entry[not(@*:morerows) or @*:morerows eq '0']/dbk:para[not(parent::dbk:footnote)]
                                                                               [following-sibling::dbk:para]//dbk:br[not($xerif)]">
    <rule>
      <text> \newline </text>
    </rule>
  </template>
  
  <template context="dbk:entry[not(@*:morerows) or @*:morerows eq '0']/dbk:para[not(parent::dbk:footnote)]
                                                                               [following-sibling::dbk:para][not($xerif)]">
    <rule>
      <xsl:next-match/>
      <text> \newline </text>
    </rule>
  </template>
  
  <template context="dbk:entry//dbk:br[every $precedent in ancestor::dbk:para[1]//node()[. &lt;&lt; current()] 
                                       satisfies (not(normalize-space($precedent)))]">
    <rule>
      <text>\leavevmode\newline
</text>  
    </rule>
  </template>

  <xsl:variable name="text-style-elements" select="'phrase', 'superscript', 'subscript'" as="xs:string+"/>

  <template context="*[local-name() = $text-style-elements]
                      [   (@css:letter-spacing and xs:double(replace(@css:letter-spacing, '[a-z]+$', '')) ge 0.3)
                       or (exists(key('style', @role)[@css:letter-spacing[xs:double(replace(., '[a-z]+$', '')) ge 0.3]]) 
                           and not(@css:letter-spacing[xs:double(replace(., '[a-z]+$', '')) lt 0.3]))]">
    <xsl:choose>
      <xsl:when test="$letter-spacing-def eq 'soul'">
        <!-- the command whose letterskip equals the phrase's declared Word
             tracking (soul's default \so would space at .25em) -->
        <xsl:variable name="tracking" as="xs:string"
                      select="replace((@css:letter-spacing, key('style', @role)/@css:letter-spacing)[1], '[a-z]+$', '')"/>
        <xsl:variable name="slot" as="xs:integer?"
                      select="index-of($docx-letterspacings, $tracking)"/>
        <xsl:choose>
          <xsl:when test="exists($slot)">
            <xsl:text>\docxso</xsl:text>
            <xsl:value-of select="substring('ABCDEFGHIJKLMNOPQRSTUVWXYZ', $slot, 1)"/>
            <xsl:text>{</xsl:text>
          </xsl:when>
          <xsl:otherwise>
            <xsl:text>\so{</xsl:text>
          </xsl:otherwise>
        </xsl:choose>
      </xsl:when>
      <xsl:otherwise>
        <xsl:text>\textls{</xsl:text>
      </xsl:otherwise>
    </xsl:choose>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="*[local-name() = $text-style-elements]
                      [   matches(@css:text-decoration-line, 'line-through')
                       or exists(key('style', @role)[matches(@css:text-decoration-line, 'line-through')])]">
    <xsl:text>\sout{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="*[local-name() = $text-style-elements]
                      [@css:text-decoration-line ne 'none' or not(@css:text-decoration-line)]
                      [   (matches(@css:text-decoration-line, 'underline') 
                       or  exists(key('style', @role)[matches(@css:text-decoration-line, 'underline')]))]
		 [not(@role eq 'Hyperlink')] 
		 [not(parent::dbk:link) and exists(..)(:if no root exists:)]
		 [not(.//dbk:link)]">
    <xsl:variable name="line-style" select="(@css:text-decoration-style, key('style', @role)/@css:text-decoration-style)[1]"></xsl:variable>
    <xsl:value-of select="if (matches($line-style,'dotted'))
                          then '\dotuline{'
                          else if (matches($line-style,'dashed'))
                               then '\dashuline{'
                               else if (matches($line-style,'double'))
                                     then  '\uuline{'
                                     else  '\uline{' "/>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="dbk:para[not(@css:font-style eq 'normal')]
                             [@css:font-style eq 'italic'
                              or exists(key('style', @role)[@css:font-style eq 'italic'])]
                             [not(ancestor::dbk:info)]">
   <xsl:param name="suppress-cca" select="tr:suppress-structure(.)" tunnel="yes" as="xs:boolean?"/>
   <xsl:param name="already-structured" tunnel="yes" as="xs:boolean?"/>
   <xsl:sequence select="tr:add-cca('Start', 'P')[not($suppress-cca) and not($already-structured)]"/>
   <rule name="textit" type="cmd" break-after="0">
      <param/>
      <xsl:choose>
        <xsl:when test="parent::*[local-name() = ('entry', 'th', 'td')][not($xerif)]
                        and following-sibling::*[1]">
          <xsl:text>\newline&#xa;</xsl:text>
        </xsl:when>
        <xsl:when test="not(   parent::dbk:footnote
                            or (parent::dbk:listitem and not(following-sibling::*)))
                        and following-sibling::*[1]">
          <xsl:text>&#xa;&#xa;</xsl:text>
        </xsl:when>
      </xsl:choose>
    </rule>
    <xsl:sequence select="tr:add-cca('End', 'P')[not($suppress-cca) and not($already-structured)]"/>
    <xsl:text>&#xa;&#xa;</xsl:text>
  </template>
  
  <template context="dbk:para[not(@css:font-weight eq 'normal')]
                             [@css:font-weight eq 'bold'
                              or (    exists(key('style', @role)[@css:font-weight eq 'bold'])
                                  and not(@css:font-weight eq 'normal'))]">
    <xsl:param name="suppress-cca" select="tr:suppress-structure(.)" tunnel="yes" as="xs:boolean?"/>
    <xsl:param name="already-structured" tunnel="yes" as="xs:boolean?"/>
    <xsl:sequence select="tr:add-cca('Start', 'P')[not($suppress-cca) and not($already-structured)]"/>
    <rule name="textbf" type="cmd">
      <param/>
      <xsl:choose>
        <xsl:when test="parent::*[local-name() = ('entry', 'th', 'td')][not($xerif)]
                        and following-sibling::*[1]">
          <xsl:text>\newline&#xa;</xsl:text>
        </xsl:when>
        <xsl:when test="not(   parent::dbk:footnote
                            or (parent::dbk:listitem and not(following-sibling::*)))
                        and following-sibling::*[1]">
          <xsl:text>&#xa;&#xa;</xsl:text>
        </xsl:when>
      </xsl:choose>
    </rule>
    <xsl:sequence select="tr:add-cca('End', 'P')[not($suppress-cca) and not($already-structured)]"/>
  </template>

  <template context="dbk:*[@css:display eq 'none']">
    <rule>
      <text>&#xa;%</text>
      <text/>
      <text>&#xa;</text>
    </rule>
  </template>
  
  <template context="dbk:para[   @css:text-align='center'
                              or exists(key('style', @role)[@css:text-align eq 'center'])]
                             [not(parent::*/@css:text-align='center')]
                             [not(@css:text-align = ('left', 'right', 'justify'))(:if centering is in style but overwritten:)]
                             [not(parent::dbk:entry or parent::*:th or parent::*:td)]">
    <xsl:text>{\centering </xsl:text>    
    <xsl:next-match/>
    <xsl:text>\par}</xsl:text>
  </template>
  
  <template context="dbk:para[   @css:text-align='right'
                              or exists(key('style', @role)[@css:text-align eq 'right'])]
                             [not(parent::*/@css:text-align='right')]
                             [not(@css:text-align = ('left', 'center', 'justify'))]
                             [not(parent::dbk:entry or parent::*:th or parent::*:td)]">
    <xsl:text>{\raggedleft </xsl:text>
    <xsl:next-match/>
    <xsl:text>\par}</xsl:text>
  </template>
  
  <template context="dbk:para[(    @css:text-align='left'
                               and key('style', @role)/@css:text-align = ('center', 'right'))
                              or (not(@css:text-align)
                                  and parent::*[local-name() = ('td', 'th', 'entry')]
                                  and following-sibling::*[1][@css:text-align[. ne 'left']])]
                             [not(parent::*/@css:text-align='left')]
                             [not(parent::dbk:entry or parent::*:th or parent::*:td)]">
    <xsl:text>{\raggedright </xsl:text>
    <xsl:next-match/>
    <xsl:text>\par}</xsl:text>
  </template>
  
  <!-- manually lowered or raised position -->
  
  <template context="dbk:phrase[(@css:top[. ne '0pt'], key('style', @role)/@css:top[. ne '0pt'])[1]]/text()">
    <rule>
      <text select="if(starts-with((parent::*/@css:top, key('style', parent::*/@role)/@css:top)[1], '-'))
                    then concat('\raisebox{', replace((parent::*/@css:top, key('style', parent::*/@role)/@css:top)[1], '^-', ''), '}')
                    else concat('\raisebox{-', (parent::*/@css:top, key('style', parent::*/@role)/@css:top)[1], '}')"/>
      <param/>
    </rule>
  </template>

  <!-- tables and figures -->

  <template context="dbk:table[not(parent::dbk:entry)]
                    |dbk:informaltable[not(parent::dbk:entry)]">
    <rule name="table" type="env">
      <text/>
    </rule>
  </template>

  <template context="dbk:figure/dbk:title|dbk:table/dbk:title">
    <rule break-before="1" name="caption" type="cmd">
      <param/>
    </rule>
  </template>

  <template context="dbk:figure">
    <rule name="figure" type="env">
      <text/>
      <text>&#xa;</text>
      <text select="concat('\label{fig:', index-of(for $i in //dbk:figure return generate-id($i), generate-id(.)), '}')"/>
    </rule>
  </template>
  
  <template context="dbk:alt"/>
  <!-- Word image metadata (docx2hub puts the image title here) renders
       nowhere in Word — suppress it like alt -->
  <template context="dbk:mediaobject/dbk:info"/>

  <!-- Word floating text boxes (VML fallback shapes with position:absolute):
       render the box's paragraphs at the Word coordinates. Offsets are relative
       to the text column start ('mso-position-*-relative:text'), so the page
       margins are added; the vertical anchor paragraph is at the top margin. -->
  <template context="w:pict[v:shape/v:textbox][contains(v:shape/@style, 'position:absolute')][contains(v:shape/@style, 'margin-left')][contains(v:shape/@style, 'margin-top')][not(ancestor::dbk:div[@role = ('docx2hub:header', 'docx2hub:footer')])]">
    <xsl:variable name="style" select="v:shape/@style" as="xs:string"/>
    <xsl:variable name="vml-left" as="xs:string"
                  select="(tokenize($style, ';')[normalize-space(substring-before(., ':')) eq 'margin-left'], '0pt')[1]"/>
    <xsl:variable name="vml-top" as="xs:string"
                  select="(tokenize($style, ';')[normalize-space(substring-before(., ':')) eq 'margin-top'], '0pt')[1]"/>
    <xsl:variable name="vml-width" as="xs:string"
                  select="(tokenize($style, ';')[normalize-space(substring-before(., ':')) eq 'width'], '0pt')[1]"/>
    <xsl:variable name="left" select="(tr:css-length-to-pt(substring-after($vml-left, ':')), 0e0)[1]" as="xs:double"/>
    <xsl:variable name="top" select="(tr:css-length-to-pt(substring-after($vml-top, ':')), 0e0)[1]" as="xs:double"/>
    <xsl:variable name="width" select="(tr:css-length-to-pt(substring-after($vml-width, ':')), 0e0)[1]" as="xs:double"/>
    <xsl:variable name="page-left" select="(tr:css-length-to-pt(/dbk:hub/@css:margin-left), 72e0)[1]" as="xs:double"/>
    <xsl:variable name="page-top" select="(tr:css-length-to-pt(/dbk:hub/@css:margin-top), 72e0)[1]" as="xs:double"/>
    <!-- VML default textbox insets: 0.1in (7.2pt) left/right, 0.05in (3.6pt)
         top/bottom (no shape in these documents declares @inset). Word starts
         the text at box + left inset and wraps at box width minus both insets;
         using the full box width made lines wrap later than Word and shifted
         every box's text a few points left of the reference. -->
    <xsl:variable name="inset-x" select="7.2e0" as="xs:double"/>
    <xsl:variable name="inset-y" select="3.6e0" as="xs:double"/>
    <!-- Word/OnlyOffice place the first line's baseline at box top + inset +
         line height - descent (descent ~ 0.25em); the paragraph-level \null
         reference line (see the para font-size template) puts the first
         baseline exactly at textblock y + line height, so the y sheds the
         descent; +1pt calibrates the reference's own sub-point overshoot -->
    <xsl:variable name="first-para" as="element(dbk:para)?"
                  select="(v:shape/v:textbox/w:txbxContent/dbk:para)[1]"/>
    <xsl:variable name="first-size" as="xs:double"
                  select="(max(for $s in ($first-para//dbk:phrase[ancestor::dbk:para[1] is $first-para]/@css:font-size,
                                       $first-para/@css:font-size,
                                       key('style', $first-para/@role)/@css:font-size)
                          return number(replace($s, '[a-z]+$', ''))[. = .]), 12e0)[1]"/>
    <!-- textpos module arguments are plain numbers (1pt modules), no units;
         the box geometry is inset by the VML default internal margins -->
    <xsl:text>\begin{textblock}{</xsl:text>
    <xsl:value-of select="round(($width - 2 * $inset-x) * 10) div 10"/>
    <xsl:text>}(</xsl:text>
    <xsl:value-of select="round(($page-left + $left + $inset-x) * 10) div 10"/>
    <xsl:text>,</xsl:text>
    <xsl:value-of select="round(($page-top + $top + $inset-y - 0.25 * $first-size + 1) * 10) div 10"/>
    <xsl:text>)\parbox[t]{</xsl:text>
    <xsl:value-of select="round(($width - 2 * $inset-x) * 10) div 10"/>
    <xsl:text>pt}{</xsl:text>
    <xsl:apply-templates select="v:shape/v:textbox/element()" mode="#current"/>
    <xsl:text>}\end{textblock}</xsl:text>
  </template>

  <template context="dbk:imagedata[@fileref]">
    <rule name="includegraphics" type="cmd">
      <!-- Word display size when declared (both dimensions, to keep the Word box
           aspect even when it differs from the pixel aspect); textwidth share only
           as a fallback -->
      <option select="concat('width=', 
                             if (@css:width[not(. eq 'auto')])
                             then @css:width
                             else concat(if(ancestor::dbk:figure)
                                         then 1 div count(ancestor::dbk:figure[last()]//*[local-name() = ('mediaobject', 'inlinemediaobject')])
                                         else 1,
                                         '\textwidth'),
                             if (@css:height[not(. eq 'auto')])
                             then concat(',height=', @css:height)
                             else '')"/>
      <param select="replace(@fileref, '([%#])', '\\$1')"/>
    </rule>
  </template>


  <template context="dbk:imagedata[@fileref][@css:position-left][@css:position-top]
                             [not(ancestor::dbk:div[@role = ('docx2hub:header', 'docx2hub:footer')])]">
    <!-- Word floating pictures (wp:anchor): place at the anchor coordinates -->
    <xsl:variable name="page-left" select="number(replace((/dbk:hub/@css:margin-left, '72pt')[1], '[a-z]+$', ''))" as="xs:double"/>
    <xsl:variable name="page-top" select="number(replace((/dbk:hub/@css:margin-top, '72pt')[1], '[a-z]+$', ''))" as="xs:double"/>
    <xsl:variable name="x" select="$page-left + number(replace(@css:position-left, '[a-z]+$', ''))" as="xs:double"/>
    <xsl:variable name="y" select="$page-top + number(replace(@css:position-top, '[a-z]+$', ''))" as="xs:double"/>
    <xsl:variable name="w" select="number(replace((@css:width[not(. eq 'auto')], '0pt')[1], '[a-z]+$', ''))" as="xs:double"/>
    <xsl:variable name="h" select="number(replace((@css:height[not(. eq 'auto')], '0pt')[1], '[a-z]+$', ''))" as="xs:double"/>
    <xsl:text>\begin{textblock}{</xsl:text>
    <xsl:value-of select="round($w * 10) div 10"/>
    <xsl:text>}(</xsl:text>
    <xsl:value-of select="round($x * 10) div 10"/>
    <xsl:text>,</xsl:text>
    <xsl:value-of select="round($y * 10) div 10"/>
    <xsl:text>)\includegraphics[width=</xsl:text>
    <xsl:value-of select="round($w * 10) div 10"/>
    <xsl:text>pt,height=</xsl:text>
    <xsl:value-of select="round($h * 10) div 10"/>
    <xsl:text>pt]{</xsl:text>
    <xsl:value-of select="replace(@fileref, '([%#])', '\\$1')"/>
    <xsl:text>}\end{textblock}</xsl:text>
  </template>
  
  <xsl:variable name="svg-images" as="element(svg:svg)*"
                select="//svg:svg"/>
  
  <template context="svg:svg">
    <!-- index by document order: svg shapes have no text nodes, so their string
         values are all equal and index-of() would return the same position for
         every one of them -->
    <xsl:variable name="svg-index" select="count($svg-images[. &lt;&lt; current()]) + 1" as="xs:integer"/>
    <xsl:variable name="svg-filename" select="concat($basename, '-svg-', $svg-index, '.svg')" as="xs:string"/>
    <xsl:variable name="svg-path" select="concat($path, '/', $svg-filename)" as="xs:string"/>
    <!-- the svg is written inside <name>.docx.tmp/word/ (d2t unzip layout, like
         the media files); the d2t wrapper converts it to PDF there (LuaLaTeX
         cannot include .svg), so the tex references the .pdf relatively through
         that path. Degenerate conversions (zero width) are written for debug
         but not referenced. -->
    <!-- anchored vector art (docx2hub remembers the wp:anchor on the parent
         imagedata): the d2s coordinates are page points, so the svg's viewBox
         offset is the art's page position — place the natural-size image there
         (textpos 1pt modules), behind whatever content follows in the flow -->
    <xsl:variable name="anchored" as="element(dbk:imagedata)?"
                  select="ancestor::dbk:imagedata[@css:position-left][@css:position-top]
                                            [not(ancestor::dbk:div[@role = ('docx2hub:header', 'docx2hub:footer')])]"/>
    <xsl:variable name="vb" select="tokenize(normalize-space(@viewBox), '\s+')" as="xs:string*"/>
    <xsl:if test="@width[number(replace(., '[a-z]+$', '')) gt 0]">
      <xsl:choose>
        <xsl:when test="exists($anchored) and count($vb) eq 4
                        and $vb[1][. castable as xs:double] and $vb[2][. castable as xs:double]">
          <xsl:text>\begin{textblock}{</xsl:text>
          <xsl:value-of select="round(number(replace(@width, '[a-z]+$', '')) * 10) div 10"/>
          <xsl:text>}(</xsl:text>
          <xsl:value-of select="round(number($vb[1]) * 10) div 10"/>
          <xsl:text>,</xsl:text>
          <xsl:value-of select="round(number($vb[2]) * 10) div 10"/>
          <xsl:text>)\includegraphics{</xsl:text>
          <xsl:value-of select="concat($basename, '.docx.tmp/word/', $basename, '-svg-', $svg-index, '.pdf')"/>
          <xsl:text>}\end{textblock}</xsl:text>
        </xsl:when>
        <xsl:otherwise>
          <rule name="includegraphics" type="cmd">
            <param select="concat($basename, '.docx.tmp/word/', $basename, '-svg-', $svg-index, '.pdf')"/>
          </rule>
        </xsl:otherwise>
      </xsl:choose>
    </xsl:if>
    <file href="{$svg-path}" encoding="utf-8" method="xml"/>
  </template>
  
  <!-- links -->
  
  <template context="dbk:link[@xlink:href][matches(@xlink:href, '^(https?|ftp):')]">
    <rule name="href" type="cmd">
      <param select="replace((@xlink:href, @url)[1], '([%#_\\])', '\\$1')"/>
      <param/>
    </rule>
  </template>

  <template context="dbk:link[@xlink:href][matches(@xlink:href, '^(https?|ftp):')]
                             [@xlink:href = replace(., '\\', '')]
                    |dbk:ulink">
    <rule name="url" type="cmd">
      <param select="replace((@xlink:href, @url)[1], '([%#])', '\\$1')"/>
    </rule>
  </template>
  
  <template context="dbk:link[@xlink:href][matches(@xlink:href, '^mailto:')]">
    <rule>
      <text/>
    </rule>
  </template>

  <!-- quotes -->

  <template context="dbk:blockquote">
    <rule name="quote" type="env" break-before="1">
      <text/>
    </rule>
  </template>
  
  <template context="dbk:para[not(parent::dbk:blockquote)][@role = ('Zitat', 'Quote')]">
    <rule name="quote" type="env" break-before="1">
      <text/>
    </rule>
  </template>

  <template context="dbk:blockquote[count(dbk:para) gt 1]">
    <rule name="quotation" type="env" break-before="1">
      <text/>
    </rule>
  </template>
  
  <!-- beamer: each slide section becomes a frame -->

  <template context="dbk:section[@role = 'slide']">
    <rule name="frame" type="env" break-before="1">
      <text/>
    </rule>
  </template>

  <template context="dbk:section[@role = 'slide']/dbk:title">
    <rule name="frametitle" type="cmd" break-before="1">
      <param/>
    </rule>
  </template>

  <template context="dbk:section[@role = 'slide']/dbk:subtitle">
    <rule name="framesubtitle" type="cmd" break-before="1">
      <param/>
    </rule>
  </template>

  <!-- itemized lists -->

  <template context="dbk:itemizedlist">
    <rule name="itemize" type="env" break-before="1">
      <text/>
    </rule>
  </template>
  
  <template context="dbk:listitem[not(parent::dbk:varlistentry)][empty(@override)]">
    <rule name="item" type="cmd" break-before="1">        
      <xsl:choose>
        <xsl:when test="   parent::dbk:itemizedlist/@mark 
                       and parent::dbk:itemizedlist/@mark != 'bullet'
                       and not($xerif)">
          <option select="xml2tex:utf2tex((), parent::dbk:itemizedlist/@mark, $charmap, (), $texregex)"/>
        </xsl:when>
        <xsl:when test="parent::dbk:itemizedlist/@mark eq 'bullet' and not($xerif)">
          <!-- Word's default bullet is the Symbol-font middle dot (F0B7), which
               Word/OnlyOffice render as a small dot, not LaTeX's full bullet -->
          <option select="'\textperiodcentered'"/>
        </xsl:when>
      </xsl:choose>
      <xsl:if test="not($a11y)"><text select="' '"/></xsl:if>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:listitem[not(parent::dbk:varlistentry)][@override]">
    <rule name="item" type="cmd" break-before="1">
      <option select="@override"/>
      <xsl:if test="not($a11y)"><text select="' '"/></xsl:if>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:orderedlist/dbk:listitem">
    <xsl:variable name="level" select="count(ancestor::*:orderedlist|ancestor::*:itemizedlist)" as="xs:integer"/>
    <xsl:variable name="level-roman" as="xs:string">
      <xsl:number value="$level" format="i"/>
    </xsl:variable>
    <xsl:variable name="pos" select="count(preceding-sibling::*) + 1" as="xs:integer"/>
    <xsl:variable name="override" select="@override" as="attribute(override)?"/>
    <xsl:variable name="numeration" select="parent::*/@numeration" as="attribute(numeration)?"/>
    <xsl:if test="    $override
                  and $numeration
                  and not(  tr:list-number-to-integer($override, $numeration) 
                          - preceding-sibling::*[1]/tr:list-number-to-integer(@override, parent::*/@numeration)
                          = 1 )
                  and $pos ne 1">
      <xsl:value-of select="concat('&#xa;\setcounter{enum', $level-roman, '}{', 
                                   xs:string(
                                               preceding-sibling::*[1]/tr:list-number-to-integer(@override, parent::*/@numeration)
                                             + tr:list-number-to-integer($override, $numeration)
                                             - preceding-sibling::*[1]/tr:list-number-to-integer(@override, parent::*/@numeration)
                                             - 1), 
                                   '}&#xa;')"/>
    </xsl:if>
    <rule name="item" type="cmd" break-before="1">
      <xsl:if test="matches(@override, '^[(\[{]?[a-z0-9]+(([a-z])*|(\.([a-z0-9])*))[)\]}]?$')">
        <option select="@override"/>
      </xsl:if>
      <xsl:if test="not($a11y)"><text select="' '"/></xsl:if>
      <text/>
    </rule>
  </template>
  
  <!-- orderedlists are handled in enum.xsl -->
  
  <!-- variable list -->
  
  <template context="dbk:variablelist">
    <xsl:variable name="max-length" as="xs:integer"
                  select="max(dbk:varlistentry/dbk:term/string-length(replace(., '\{\\slash\}', '/')))"/>
    <xsl:variable name="longest-word" as="xs:string" 
                  select="dbk:varlistentry[dbk:term[string-length(replace(., '\{\\slash\}', '/')) eq $max-length]][1]/dbk:term"/>
    <rule name="description" type="env" break-before="1">
      <!-- https://github.com/transpect/docx2tex/issues/111
           optional argument only valid in xerif -->
      <xsl:if test="$xerif">
        <option select="$longest-word"/>
      </xsl:if>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:term[parent::dbk:varlistentry]">
    <rule name="item" type="cmd" break-before="1">
      <option/>
    </rule>
  </template>  

  <!-- text styles -->
  
  <template context="*[local-name() = $text-style-elements]
                      [@css:color or key('style', @role)/@css:color]
                      [not(every $i in  tr:hex-rgb-color-to-ints((@css:color, key('style', @role)/@css:color)[1]) 
                           satisfies $i &lt; 35)]
                      [not(@role = ('ZFinlineequation', 'ZFequation', 'ZFVerweis', 'ZFCaption', 'NOTE-CE', 'NOTE-TS', 'Hyperlink'))]
                      [not(preceding-sibling::*[@role = ('ZFinlineequation', 'ZFequation')])]
                      [not(following-sibling::*[@role = ('ZFinlineequation', 'ZFequation')])]
                      [not(ancestor-or-self::dbk:link)]">
    <xsl:text>\textcolor{</xsl:text>
    <xsl:value-of select="for $color-code in (@css:color, key('style', parent::*/@role)/@css:color)[1] 
                          return if(exists(tr:color-hex-rgb-to-keyword($color-code)))
                                 then tr:color-hex-rgb-to-keyword($color-code)[1]
                                 else concat('color-', upper-case(substring-after($color-code, '#')))"/>
    <xsl:text>}{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>

  <template context="*[local-name() = $text-style-elements]
                      [@css:font-style eq 'normal']
                      [   exists(ancestor-or-self::*[@css:font-style = 'italic'][1])
                       or (some $role in ancestor-or-self::*[@role]/@role
                           satisfies exists(key('style', $role)[@css:font-style eq 'italic']))]">
    <xsl:text>\textup{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="*[local-name() = $text-style-elements]
                      [@css:font-weight eq 'normal']
                      [   exists(ancestor-or-self::*[@css:font-weight = 'bold'][1])
                       or (some $role in ancestor-or-self::*[@role]/@role
                           satisfies exists(key('style', $role)[@css:font-weight eq 'bold']))]">
    <xsl:text>\textnormal{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="*[local-name() = $text-style-elements]
                      [   @css:font-style eq 'italic' 
                       or (    exists(key('style', @role)[@css:font-style eq 'italic'])
                           and not(@css:font-style eq 'normal'))]">
    <xsl:value-of select="if (count(descendant::dbk:para) gt 1) then '{\itshape ' else '\textit{'"/>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="*[local-name() = $text-style-elements]
                      [   @css:font-weight eq 'bold'
                       or (    exists(key('style', @role)[@css:font-weight eq 'bold'])
                           and not(@css:font-weight eq 'normal'))]">
    <xsl:value-of select="if (count(descendant::dbk:para) gt 1) then '{\bfseries ' else '\textbf{'"/>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="*[local-name() = $text-style-elements]
                      [@css:font-style eq 'italic' or @css:font-weight eq 'bold']
                      [matches(., '^[&#x393;&#x394;&#x398;&#x39b;&#x39e;&#x3a0;&#x3a3;&#x3a5;&#x3a6;&#x3a8;-&#x3be;&#x3c0;-&#x3f6;]$')]">
    <rule>
      <text/>
    </rule>
  </template>
  
  <template context="*[local-name() = $text-style-elements]
                      [   @css:font-family = ('Consolas', 'Courier New', 'Courier')
                       or exists(key('style', @role)[@css:font-family = ('Consolas', 'Courier New', 'Courier')])]">
    <xsl:text>\texttt{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>

  <template context="*[local-name() = $text-style-elements]
                      [    (@css:font-variant eq 'small-caps'
                            or exists(key('style', @role)[@css:font-variant eq 'small-caps']))
                       and not(@css:text-transform eq 'uppercase'
                               or exists(key('style', @role)[@css:text-transform eq 'uppercase']))]">
    <xsl:value-of select="if (count(descendant::dbk:para) gt 1) then '{\scshape ' else '\textsc{'"/>

    <!-- https://mattermost.le-tex.de/letexml/pl/oky434bc6bnqfmzs3zk8nf8e4o-->
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>

  <template context="*[local-name() = $text-style-elements]
                      [not(@css:background-color eq '#FFFFFF')
                                and (@css:background-color
                                or exists(key('style', @role)[@css:background-color and not(@css:background-color eq '#FFFFFF') ]))]
                               [not(.//dbk:footnote)]">
    <!-- \fboxsep=0pt: Word text highlight has no padding around the highlighted run;
         the default 3pt \fboxsep inserted visual gaps between adjacent runs
         (e.g. "Month 20 YY" instead of "Month 20YY") -->
    <xsl:text>{\fboxsep0pt\colorbox{</xsl:text>
    <xsl:value-of select="for $color-code in (@css:background-color, key('style', @role)/@css:background-color)[1]
                          return if(exists(tr:color-hex-rgb-to-keyword($color-code)))
                                 then tr:color-hex-rgb-to-keyword($color-code)[1]
                                 else concat('color-', upper-case(substring-after($color-code, '#')))"/>
    <xsl:text>}{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}}</xsl:text>
  </template>

  <!-- regular superscript and subscripts -->
  
  <template context="dbk:subscript
                    |dbk:phrase[not(dbk:footnote)]
                               [not(parent::dbk:subscript)]
                               [key('style', @role)[@remap eq 'subscript']]">
    <xsl:text>\textsubscript{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="dbk:superscript[not(dbk:footnote)]
                    |dbk:phrase[not(dbk:footnote)]
                               [not(parent::dbk:superscript)]
                               [key('style', @role)[@remap eq 'superscript']]">
    <xsl:text>\textsuperscript{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>

  <!-- headlines -->
  
  <template context="dbk:para[@docx2tex:config eq 'headline']/dbk:phrase[@role eq 'docx2tex:identifier']">
    <rule>
      <text/>
      <text select="' '"/>
    </rule>
  </template>

  <template context="dbk:para[@role = ('berschrift1', 'headline1', 'heading1', 'Heading1')]">
    <rule break-after="2" name="chapter" type="cmd">
      <param/>
    </rule>
  </template>

  <template context="dbk:para[@role = ('berschrift2', 'headline2', 'heading2', 'Heading2')]">
    <rule break-after="2" name="section" type="cmd">
      <param/>
    </rule>
  </template>

  <template context="dbk:para[@role = ('berschrift3', 'headline3', 'heading3', 'Heading3')]">
    <rule break-after="2" name="subsection" type="cmd">
      <param/>
    </rule>
  </template>

  <template context="dbk:para[@role = ('berschrift4', 'headline4', 'heading4', 'Heading4')]">
    <rule break-after="2" name="subsubsection" type="cmd">
      <param/>
    </rule>
  </template>

  <template context="dbk:para[@role = ('berschrift5', 'headline5', 'heading5', 'Heading5')]">
    <rule break-after="2" name="paragraph" type="cmd">
      <param/>
    </rule>
  </template>

  <template context="dbk:para[matches(@role, '(berschrift|headline|heading)[6-9]', 'i')]">
    <rule break-after="2" name="subparagraph" type="cmd">
      <param/>
    </rule>
  </template>

  <xsl:variable name="footnote-ids" as="xs:string*" 
                select="for $i in $footnotes return (if ($i/@xml:id) then $i/@xml:id else generate-id($i))"/>

  <template context="dbk:footnote">
    <xsl:variable name="pos" as="xs:integer" 
                  select="index-of($footnote-ids, (if (@xml:id) then @xml:id else generate-id()))"/>
    <xsl:variable name="preceding-regular-fn-count" as="xs:integer" 
                  select="count(
                                for $i in (1 to $pos) 
                                return $footnotes[$i][not(.//dbk:phrase[@role eq 'hub:identifier'][1][@xreflabel])]
                                )"/>
    <xsl:value-of select=".//dbk:phrase[@role eq 'hub:identifier'][1]
                            /@xreflabel/concat('\renewcommand*{\thefootnote}{',
                                                    if(matches(., '\*'))      then '\fnsymbol'
                                               else if(matches(., 'ivxlcdm')) then '\roman'
                                               else if(matches(., 'IVXLCDM')) then '\Roman'
                                               else if(matches(., '[a-z]'))   then '\alph'
                                               else if(matches(., '[A-Z]'))   then '\Alph'
                                               else if(matches(., '[\*†‡§]')) then '\fnsymbol'
                                               else                                '\arabic',
                                               '{footnote}}')"/>
    <rule name="footnote" type="cmd">
      <param/>
    </rule>
    <xsl:value-of select=".//dbk:phrase[@role eq 'hub:identifier'][1]/@xreflabel/concat(
                                                                                        '\renewcommand*{\thefootnote}{\arabic{footnote}}',
                                                                                        '\setcounter{footnote}{',
                                                                                        xs:string($preceding-regular-fn-count),
                                                                                        '}'
                                                                                        )"/>
  </template>
  
  <template context="dbk:informaltable//dbk:footnote
                    |dbk:table//dbk:footnote">
    <rule name="footnotemark" type="cmd">
      <param select="()"/>
    </rule>
  </template>
  
  <template context="dbk:informaltable[.//dbk:footnote]
                    |dbk:table[.//dbk:footnote]">
    <xsl:next-match/>
    <xsl:for-each select=".//dbk:footnote">
      <xsl:value-of select="concat('\footnotetext[', index-of($footnotes, .), ']{')"/>
      <xsl:apply-templates mode="#current"/>
      <xsl:text>}&#xa;</xsl:text>
    </xsl:for-each>
  </template>

  <template context="dbk:footnote/dbk:para[following-sibling::dbk:para]">
    <xsl:param name="suppress-cca" select="tr:suppress-structure(.)" tunnel="yes" as="xs:boolean?"/>
    <xsl:param name="already-structured" tunnel="yes" as="xs:boolean?"/>
    <rule break-after="1">
      <text select="tr:add-cca('Start', 'P')[not($suppress-cca) and not($already-structured)]"/>
      <text/>
      <text select="tr:add-cca('End', 'P')[not($suppress-cca) and not($already-structured)]"/>
    </rule>
  </template>
  
  <template context="dbk:footnote[count(*) eq 1]/dbk:para">
   <xsl:param name="suppress-cca" select="tr:suppress-structure(.)" tunnel="yes" as="xs:boolean?"/>
   <xsl:param name="already-structured" tunnel="yes" as="xs:boolean?"/>
   <rule>
      <text select="tr:add-cca('Start', 'P')[not($suppress-cca) and not($already-structured)]"/>
      <text/>
      <text select="tr:add-cca('End', 'P')[not($suppress-cca) and not($already-structured)]"/>
    </rule>
  </template>
  
  <!-- remove leading whitespace in footnotes -->
    
  <template context="dbk:footnote/dbk:para[1]//text()[preceding-sibling::node()[1][@role eq 'hub:identifier']][starts-with(., ' ')]
                    |dbk:footnote/dbk:para[1]//dbk:phrase[preceding-sibling::node()[1][@role eq 'hub:identifier']][starts-with(., ' ')]/text()[1]
                    |dbk:footnote/dbk:para[1]/dbk:phrase[preceding-sibling::*[1][@role eq 'hub:identifier']]
                                                        [preceding-sibling::node()[1][not(normalize-space())]]
                                                        [starts-with(., ' ')]/text()[1]">
    <rule>
      <text select="replace(string-join(xml2tex:utf2tex(.., ., $charmap, (), $texregex), ''), '^\s', '')"/> 
    </rule>
  </template>
    
  <template context="dbk:footnote/dbk:para[1]//text()[preceding-sibling::node()[1][@role eq 'hub:identifier']]
                                                     [not(normalize-space())]"/>  
  
  <!-- remove footnote numbers and whitespace -->
  
  <template context="dbk:footnote//dbk:phrase[@role eq 'hub:separator']"/>

  <template context="dbk:footnote/dbk:para[1]/dbk:phrase[@role eq 'hub:identifier'][1]
                                                        [not(descendant-or-self::dbk:anchor 
                                                             or descendant-or-self::processing-instruction())]
                                                         //text()[1]">
    <xsl:value-of select="replace(., '^[\d*†‡§i\s]+\.?\s*', '', 'i')"/>
  </template>
  
  <template context="dbk:footnote/dbk:para/*[1][local-name() eq 'superscript'][matches(., '\d+')]"/>

  <!-- equations -->

  <template context="dbk:inlineequation[not(ancestor::dbk:superscript or ancestor::dbk:subscript )]
                   | dbk:equation[ancestor::dbk:table or ancestor::dbk:informaltable]
                   | dbk:informalequation[ancestor::dbk:table or ancestor::dbk:informaltable]">
    <rule>
      <text select="'$'"/>
      <text select="node() except dbk:alt, dbk:alt"/>
      <text select="'$'"/>
    </rule>
  </template>

  <template context="dbk:equation[@condition eq 'numbered']
                                 [not(ancestor::dbk:table or ancestor::dbk:informaltable)]">
    <xsl:param name="suppress-cca" select="tr:suppress-structure(.)" tunnel="yes" as="xs:boolean?"/>
    <xsl:param name="already-structured" tunnel="yes" as="xs:boolean?"/>
    <rule name="equation" type="env" break-before="1">
      <text>&#xa;</text>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:equation[not(@condition eq 'numbered')]
                                 [not(ancestor::dbk:table or ancestor::dbk:informaltable)]
                    | dbk:informalequation[not(ancestor::dbk:table or ancestor::dbk:informaltable)]">
    <xsl:param name="suppress-cca" select="tr:suppress-structure(.)" tunnel="yes" as="xs:boolean?"/>
    <xsl:param name="already-structured" tunnel="yes" as="xs:boolean?"/>
    <rule name="equation*" type="env" break-before="1">
      <text>&#xa;</text>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:equation[@condition eq 'numbered'][not(ancestor::dbk:table or ancestor::dbk:informaltable)]
                                 [not(some $i in processing-instruction() satisfies contains($i, '\begin{array}'))]
                                 [some $i in processing-instruction() satisfies contains($i, '\\')]">
    <rule name="align" type="env" break-before="1">
      <text>&#xa;</text>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:equation[not(@condition eq 'numbered')]
                                 [not(ancestor::dbk:table or ancestor::dbk:informaltable)]
                                 [not(some $i in processing-instruction() satisfies contains($i, '\begin{array}'))]
                                 [some $i in processing-instruction() satisfies contains($i, '\\')]">
    <rule name="align*" type="env" break-before="1">
      <text>&#xa;</text>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:equation[$a11y]/dbk:alt|dbk:inlineequation[$a11y]/dbk:alt">
    <!-- https://redmine.le-tex.de/projects/transpect-typesetter/wiki/Accessibiliy_Features#Math-->
    <rule name="ccaAddAltText" type="cmd" break-before="0"  break-after="0">
      <!-- escaping comments and curly brackets is vital, leave other characters untouched (exlude alt texts from escaping bad chars!)-->
      <param select="replace(., '(^|[^\\])([\{\}%])', '$1\\$2')"/>
    </rule>
    <xsl:if test="..[self::dbk:equation]"><xsl:text>%&#xa;</xsl:text></xsl:if>
  </template>
  
  <template context="dbk:equation[not(@condition eq 'numbered')]
                                 [not(ancestor::dbk:table or ancestor::dbk:informaltable)]
                                 [not(some $i in processing-instruction() satisfies contains($i, '\begin{array}'))]
                                 [some $i in processing-instruction() satisfies contains($i, '\\')]">
    <rule name="align*" type="env" break-before="1">
      <text>&#xa;</text>
      <text/>
    </rule>
  </template>
  
  <template context="processing-instruction('tr')[starts-with(., 'M2M_212')]">
    <rule>
      <text>% D2T: Equation not converted. See log for details!&#xa;</text>
    </rule>
  </template>
  
  <template context="processing-instruction('d2t')[starts-with(., 'D2T 001')]">
    <rule>
      <text>% D2T: Empty equation removed!&#xa;</text>
    </rule>
  </template>
  
  <!-- replace tabs -->
  
  <!-- resolve tab when it's the begin of a term or a tab definition -->
  
  <template context="dbk:term/node()[1][self::dbk:tab]
                    |dbk:tabs"/>
  
  
  <template context="dbk:tab
                    |dbk:phrase[@role eq 'tab']">
    <rule>
      <text>&#x9;</text>
    </rule>
  </template>
  
  <template context="dbk:tab[@xml:space eq 'preserve']
                            [preceding-sibling::dbk:inlineequation and following-sibling::dbk:inlineequation]">
    <rule>
      <text>{\quad}</text>
    </rule>
  </template>
  
  <template context="dbk:footnote//dbk:tab[@role eq 'hub:separator']"/>
  
  <!-- tagged private use area -->
  <template context="dbk:phrase[@role eq 'unicode-private-use']">
    <rule name="privateuse" type="cmd">
      <param/>
    </rule>
  </template>

  <!-- comments -->

  <template context="dbk:annotation">
    <rule break-after="1">
      <text select="concat(if(parent::*/following-sibling::node()[1][self::text()][matches(., '^\s')])
                           then ' '
                           else '',
                           '%', string-join(.//node(), 
                           ' '))"/>
    </rule>
  </template>

  <!-- toc -->
  
  <template context="dbk:para[@role = ('Inhaltsverzeichnisberschrift', 'TOC Heading')]">
    <rule break-after="2">
      <text select="concat('\renewcommand{\contentsname}{', ., '}')"/>
    </rule>
  </template>
  
  <template context="dbk:div[@role eq 'hub:toc']">
    <rule break-after="2">
      <!-- Word starts the table of contents at the top of a page; with straight
           (non page-breaking) chapters the LaTeX TOC must break explicitly -->
      <text>\clearpage&#xa;\tableofcontents</text>
    </rule>
  </template>

  <!-- indexterms -->

  <xsl:template match="dbk:phrase[   @css:font-style   eq 'italic'     or exists(key('style', @role)[@css:font-style   eq 'italic'])
                                  or @css:font-weight  eq 'bold'       or exists(key('style', @role)[@css:font-weight  eq 'bold'])
                                  or @css:font-variant eq 'small-caps' or exists(key('style', @role)[@css:font-variant eq 'small-caps'])
                                  or @css:font-style   eq 'normal'
                                  or @css:font-weight  eq 'normal']
                                 [following-sibling::node()[1][self::dbk:indexterm]]" mode="escape-bad-chars">
    <xsl:copy>
      <xsl:apply-templates select="@*, node()" mode="#current"/>
      <xsl:copy-of select="following-sibling::dbk:indexterm[1]"/>
    </xsl:copy>
  </xsl:template>
  
  <xsl:template match="dbk:indexterm[preceding-sibling::node()[1][self::dbk:phrase[   @css:font-style   eq 'italic'     or exists(key('style', @role)[@css:font-style   eq 'italic'])
                                                                                   or @css:font-weight  eq 'bold'       or exists(key('style', @role)[@css:font-weight  eq 'bold'])
                                                                                   or @css:font-variant eq 'small-caps' or exists(key('style', @role)[@css:font-variant eq 'small-caps'])
                                                                                   or @css:font-style   eq 'normal'
                                                                                   or @css:font-weight  eq 'normal']]]" mode="escape-bad-chars"/>
  
  <template context="dbk:indexterm[not(@type)]">
    <rule name="index" type="cmd">
      <xsl:call-template name="index-content"/>
    </rule>
  </template>

  <template context="dbk:indexterm[@type]">
    <xsl:param name="as-option" as="xs:boolean?" tunnel="yes"/>
    <!-- param as option is automatically set in xml2tex if an option was triggered -->
    <xsl:if test="$as-option"><xsl:text>{</xsl:text></xsl:if>
    <rule name="index" type="cmd">
      <option select="@type"/>
      <xsl:call-template name="index-content"/>
    </rule>
    <xsl:if test="$as-option"><xsl:text>}</xsl:text></xsl:if>
  </template>
  
  <xsl:template name="index-content">
    <xsl:variable name="page-number-style" as="xs:string*">
      <xsl:choose>
        <xsl:when test="matches(@role, 'hub:pagenum-bold')">
          <xsl:text>|textbf</xsl:text>
        </xsl:when>
        <xsl:when test="matches(@role, 'hub:pagenum-italic')">
          <xsl:text>|textit</xsl:text>
        </xsl:when>  
      </xsl:choose>
    </xsl:variable>
    <xsl:choose>
      <xsl:when test="@class eq 'startofrange'">
        <text>{</text>
        <text/>
        <text>|(</text>
        <xsl:sequence select="$page-number-style"/>
        <xsl:text>}</xsl:text>
      </xsl:when>
      <xsl:when test="@class eq 'endofrange'">
        <text>{</text>
        <text select="key('item-by-id', @startref)/node()"/>
        <text>|)</text>
        <xsl:sequence select="$page-number-style"/>
        <xsl:text>}</xsl:text>
      </xsl:when>
      <xsl:otherwise>
        <text>{</text>
        <xsl:apply-templates mode="xml2tex"/>
        <xsl:sequence select="$page-number-style"/>
        <xsl:text>}</xsl:text>
      </xsl:otherwise>
    </xsl:choose>
  </xsl:template>
    
  <template context="dbk:indexterm[not(normalize-space())][not(@class)]"/>
  
  <template context="dbk:primary">
    <rule>
      <xsl:if test="@sortas[normalize-space()]">
        <text select="concat(@sortas, '@')"/>
      </xsl:if>
      <text/>
    </rule>
  </template>
  
  <template context="*[local-name() = ('primary', 
                                       'secondary', 
                                       'tertiary', 
                                       'quaternary', 
                                       'quinary', 
                                       'senary', 
                                       'septenary', 
                                       'octonary', 
                                       'nonary', 
                                       'denary')]
                      [@css:font-weight eq 'bold'
                       or (    exists(key('style', @role)[@css:font-weight eq 'bold'])
                           and not(@css:font-weight eq 'normal'))]">
    <xsl:text>\textbf{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="*[local-name() = ('primary', 
                                       'secondary', 
                                       'tertiary', 
                                       'quaternary', 
                                       'quinary', 
                                       'senary', 
                                       'septenary', 
                                       'octonary', 
                                       'nonary', 
                                       'denary')]
                      [@css:font-style eq 'italic'
                       or exists(key('style', @role)[@css:font-style eq 'italic'])]">
    <xsl:text>\textit{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="dbk:secondary
                    |dbk:tertiary
                    |dbk:quaternary
                    |dbk:quinary
                    |dbk:senary
                    |dbk:septenary
                    |dbk:octonary
                    |dbk:nonary
                    |dbk:denary">
    <rule>
      <text>!</text>
      <xsl:if test="@sortas[normalize-space()]">
        <text select="concat(@sortas, '@')"/>
      </xsl:if>
      <text/>
    </rule>
  </template>
  
  <xsl:variable name="see-regex" as="xs:string" 
                select="'^(see\salso|see|voir|véase|vedi|ver|siehe\sauch|siehe|请参阅|ראה|انظر|참조)\p{Zs}*(.*?)$'"/>
  
  <xsl:variable name="see-also-regex" as="xs:string" 
                select="'^(((see\s)?also)|((siehe\s)?auch))$'"/>
  
  <template context="dbk:see|dbk:seealso">
    <rule>
      <text select="if(   matches(string-join(dbk:phrase[@css:font-style eq 'italic'], ' '), $see-also-regex)
                       or self::dbk:seealso)
                     then '|seealso{' 
                     else '|see{'"/>
      <text select="node() except node()[matches(., $see-also-regex)]"/>
      <text>}</text>
    </rule>
  </template>

  <template context="*[local-name() = ('see', 'seealso')]/dbk:phrase[@css:font-style eq 'italic']
                                                                    [matches(., $see-regex) or matches(., $see-also-regex)]
                    |*[local-name() = ('see', 'seealso')]/text()[matches(., $see-regex) or matches(., $see-also-regex)]">
    <xsl:value-of select="replace(replace(., '!', '&quot;!'), 
                                  $see-regex, 
                                  '$2', 
                                  'i')"/>
    
  </template>

  <template context="*[local-name() = ('see', 'seealso')]/text()[preceding-sibling::node()[1][self::dbk:phrase[@css:font-style eq 'italic']]
                                                                                             [matches(., $see-regex) or matches(., $see-also-regex)]]">
    <xsl:value-of select="replace(., '^\s+', '')"/>
  </template>

    
  
  <template context="*[local-name() = ('see', 'seealso')]/text()[preceding-sibling::*[1][self::dbk:phrase[@css:font-style eq 'italic'][matches(., $see-regex)]]]">
    <xsl:value-of select="replace(replace(., '!', '&quot;!'), '^\p{Zs}+', '')"/>
  </template>
  
  <template context="*[local-name() = ('primary', 
                                       'secondary', 
                                       'tertiary', 
                                       'quaternary', 
                                       'quinary', 
                                       'senary', 
                                       'septenary', 
                                       'octonary', 
                                       'nonary', 
                                       'denary')]//text()[matches(., '[|!@&quot;]')]">
    <rule>
      <text select="replace(replace(., '([!@&quot;])', '&quot;$1'),
                            '\|',
                            '\\textbar{}')"/>
    </rule>
  </template>
  
  <template context="dbk:index[not(.//dbk:indexentry)]">
    <rule name="printindex" type="cmd" break-after="1">
      <option select="@remap"/>
      <text select="concat('%%%', @remap)"/>
    </rule>
  </template>
  
  <!-- static index -->
  
  <template context="dbk:index[.//dbk:indexentry]">
    <xsl:variable name="index-path" as="xs:string"
                  select="concat($path, '/', $basename, '.', @remap/concat(., '_'), 'ind')"/>
    <xsl:if test="dbk:title and not(@remap)">
      <xsl:value-of select="concat('\renewcommand{\indexname}{', dbk:title, '}&#xa;')"/>
    </xsl:if>
    <rule name="printindex" type="cmd" break-after="1">
      <option select="@remap"/>
    </rule>
    <file href="{$index-path}" encoding="utf-8"/>
  </template>
  
  <template context="dbk:index[.//dbk:indexentry]/node()">
    <xsl:apply-templates mode="#current"/>
  </template>
  
  <template context="dbk:index[.//dbk:indexentry]/dbk:title | dbk:index/dbk:info"/>

  <template context="dbk:index[.//dbk:indexentry]//dbk:indexdiv">
    <xsl:if test="not(preceding-sibling::dbk:indexdiv)">
      <xsl:value-of select="string-join(('\begin{theindex}',
                                         '  \providecommand*\lettergroupDefault[1]{}',
                                         '  \providecommand*\lettergroup[1]{%',
                                         '    \par\textbf{#1}\par',
                                         '    \nopagebreak',
                                         '  }'), 
                                        '&#xa;')"/>
    </xsl:if>
    <xsl:value-of select="concat('&#xa;&#x20;&#x20;\indexspace&#xa;&#xa;&#x20;&#x20;\lettergroup{', dbk:title, '}\nopagebreak&#xa;&#xa;')"/>
    <xsl:apply-templates select="dbk:indexentry" mode="#current"/>
    <xsl:if test="not(following-sibling::dbk:indexdiv)">
      <xsl:text>\end{theindex}</xsl:text>
    </xsl:if>
  </template>
  
  <template context="dbk:indexentry/dbk:primaryie">
    <xsl:text>&#x20;&#x20;</xsl:text>
    <rule name="item" type="cmd" break-after="1">
      <text>&#x20;</text>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:indexentry/dbk:secondaryie">
    <xsl:text>&#x20;&#x20;</xsl:text>
    <rule name="subitem" type="cmd" break-after="1">
      <text>&#x20;</text>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:indexentry/dbk:tertiaryie">
    <xsl:text>&#x20;&#x20;</xsl:text>
    <rule name="subsubitem" type="cmd" break-after="1">
      <text>&#x20;</text>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:indexentry/dbk:quaternaryie
                    |dbk:indexentry/dbk:quinaryie
                    |dbk:indexentry/dbk:senaryie
                    |dbk:indexentry/dbk:septenaryie
                    |dbk:indexentry/dbk:octonaryie
                    |dbk:indexentry/dbk:nonaryie
                    |dbk:indexentry/dbk:denaryie">
    <xsl:text>&#x20;&#x20;</xsl:text>
    <rule name="subsubsubitem" type="cmd" break-after="1">
      <text>&#x20;</text>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:seeie">
    <rule name="see" type="cmd" break-after="1">
      <param/>
      <param select="()"/>
    </rule>
  </template>
  
  <template context="dbk:seealsoie">
    <rule name="seealso" type="cmd" break-after="1">
      <param/>
      <param select="()"/>
    </rule>
  </template>
  
  <template context="dbk:indexentry//dbk:xref
                    |dbk:seeie//dbk:xref
                    |dbk:seealsoie//dbk:xref">
    <rule>
      <text select="replace(@xlink:href, '^page-', '')"/>
    </rule>
  </template>

  <!-- index listing -->
  
  <template context="dbk:para[@role = ('Index_Heading', 'Indexberschrift')]"/>
  
  <template context="dbk:div[@role eq 'hub:index']">
    <rule break-after="2">
      <text select="if(//dbk:para[@role = ('Index_Heading', 'Indexberschrift')][string-length() gt 0]) 
                    then concat('\renewcommand{\indexname}{', 
                                //dbk:para[@role = ('Index_Heading', 'Indexberschrift')][string-length() gt 0][1], 
                                '}&#xa;\printindex')
                    else '\printindex'"/>
    </rule>
  </template>
  
  <!-- textbox -->
  
  <template context="dbk:sidebar">
    <rule break-after="2" break-before="1">
      <text>\fbox{\begin{minipage}[t]{0.8\textwidth}</text>
      <text/>
      <text>\end{minipage}}</text>
    </rule>
  </template>
  
  <!-- babel support -->
  
  <template context="dbk:phrase[@xml:lang]
                               [normalize-space(.)]
                               [xml2tex:lang-to-babel-lang(@xml:lang)]
                               [not(parent::dbk:link)]
                               [every $lang in @xml:lang
                                satisfies not(ancestor::*[@xml:lang][1]/@xml:lang = $lang)]
                               [not(ancestor::*[self::dbk:entry|self::dbk:term|self::dbk:div[@role = 'transcription']])]
                               [not((*/local-name() = ('link', 'mediaobject', 'inlinemediaobject'))
                                    and 
                                    not(normalize-space(string-join(.//text()[not(ancestor::*/local-name() = ('link', 'mediaobject', 'inlinemediaobject'))], '')))
                                   )]">
    <xsl:param name="suppress-foreign-lang" as="xs:boolean?" tunnel="yes"/>
    <xsl:param name="protect-lang" as="xs:boolean?" tunnel="yes" select="false()"/>
    <xsl:value-of select="if ($suppress-foreign-lang) 
                          then () 
                          else concat('\protect'[$protect-lang], '\foreignlanguage{', xml2tex:lang-to-babel-lang(@xml:lang), '}{')"/>
    <xsl:next-match/>
    <xsl:value-of select="if ($suppress-foreign-lang) 
                          then ()
                          else '}'"/>
  </template>
  
  <template context="*[local-name() = $text-style-elements]
                      [@css:text-transform eq 'uppercase'
                       or exists(key('style', @role)[@css:text-transform eq 'uppercase'])]">
    <xsl:text>\uppercase{</xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>
  
  <template context="dbk:para[xml2tex:lang-to-babel-lang(@xml:lang)[normalize-space()]]
                             [not(@xml:lang = $langs[1])]" priority="1000">
    <xsl:param name="suppress-select-lang" as="xs:boolean" select="false()"/>
    <xsl:variable name="protect-lang" as="xs:boolean" select="ancestor::dbk:title or ancestor::dbk:caption or ancestor::dbk:legalnotice"/>
    <xsl:if test="not($suppress-select-lang) and not(preceding-sibling::*[1]/@xml:lang = current()/@xml:lang)">
      <xsl:value-of select="concat('\protect'[$protect-lang],'\selectlanguage{', xml2tex:lang-to-babel-lang(@xml:lang), '}%&#xa;')"/>
    </xsl:if>
    <xsl:next-match/>
    <xsl:if test="not($suppress-select-lang)">
      <xsl:if test="not(parent::dbk:footnote) 
                    and 
                    (not(following-sibling::*[1]/@xml:lang = current()/@xml:lang) or following-sibling::*[1][self::dbk:bibliomixed])">
        <xsl:value-of select="concat('\selectlanguage{', xml2tex:lang-to-babel-lang($langs[1]), '}')"/>
      </xsl:if>
    </xsl:if>
  </template>
  
  <!-- bibliographies -->
  
  <template context="dbk:bibliography">
    <rule name="thebibliography" type="env" break-after="2" break-before="1">
      <param>0</param>
      <text/>
    </rule>
  </template>
  
  <template context="dbk:bibliodiv">
    <rule name="bibsect" type="env">
      <param select="dbk:title"/>
      <text select="node() except dbk:title"/>
    </rule>
  </template>
  
  <template context="dbk:bibliomixed">
    <xsl:param name="suppress-cca" select="tr:suppress-structure(.)" tunnel="yes" as="xs:boolean?"/>
    <xsl:param name="already-structured" tunnel="yes" as="xs:boolean?"/>
    <rule name="bibitem" type="cmd" break-before="1">
      <option select="concat('bib-', position())"/>
      <param select="@xreflabel"/>
      <text select="tr:add-cca('Start', 'P')[not($suppress-cca) and not($already-structured)]"/>
      <text/>
      <text select="tr:add-cca('End', 'P')[not($suppress-cca) and not($already-structured)]"/>
    </rule>
  </template>
  
  <template context="dbk:biblioref
                    |dbk:citation">
    <rule name="cite" type="cmd">
      <param select="string-join(tokenize((@linkends, @linkend)[1], '\s'), ',')"/>
    </rule>  
  </template>
  
  <template context="dbk:bibliography[@role = ('Citavi', 'CSL')]">
    <xsl:variable name="bibtex-path" select="concat($path, '/', $basename, '-bibtex.bib')" as="xs:string"/>
    <rule name="bibliography" type="cmd" break-after="2" break-before="2">
      <param select="concat($basename, '-bibtex')"/>
    </rule>
    <file href="{$bibtex-path}" encoding="utf-8"/>
  </template>
  
  <template context="dbk:bibliography/dbk:biblioentry">
    <xsl:variable name="main-set" as="element(dbk:biblioset)"
                  select="(dbk:biblioset[@relation eq 'article'], dbk:biblioset[@relation eq 'inproceedings'], dbk:biblioset)[1]"/>
    <xsl:value-of select="concat('@', 
                                 if(matches($main-set/@relation, 'thesis')) 
                                   then 'phdthesis'
                                 else if(matches($main-set/@relation, 'broadcast')) 
                                   then 'misc'
                                 else if($main-set/@relation) then $main-set/@relation
                                 else 'misc', 
                                 '{',
                                 @xml:id,
                                 ',&#xa;')"/>
    <xsl:apply-templates mode="#current"/>
    <xsl:text>}&#xa;</xsl:text>
  </template>
  
  <template context="dbk:biblioentry[dbk:biblioset[@relation eq 'article']]/dbk:biblioset[@relation = ('journal', 'incollection', 'inproceedings')]">
    <xsl:value-of select="concat('journal={', ' ', dbk:title, '},&#xa;')"/>
  </template>
  
  <template context="dbk:biblioset/*"/>
  
  <template context="dbk:authorgroup">
    <xsl:text>author={</xsl:text>
    <xsl:for-each select="dbk:author">
      <xsl:apply-templates mode="#current"/>
      <xsl:if test="position() ne last()">
        <xsl:text> and </xsl:text>
      </xsl:if>
    </xsl:for-each>
    <xsl:text>},&#xa;</xsl:text>
    <xsl:if test="dbk:editor">
      <xsl:text>editor={</xsl:text>
      <xsl:for-each select="dbk:editor">
        <xsl:apply-templates mode="#current"/>
        <xsl:if test="position() ne last()">
          <xsl:text> and </xsl:text>
        </xsl:if>
      </xsl:for-each>
      <xsl:text>},&#xa;</xsl:text>
    </xsl:if>
  </template>
  
  <template context="dbk:biblioentry//dbk:personname">
    <xsl:value-of select="string-join((dbk:firstname, dbk:othername[starts-with(@role, 'middle')], dbk:surname), ' ')"/>
  </template>
  
  <template context="dbk:biblioentry//dbk:author
                    |dbk:biblioentry//dbk:editor">
    <xsl:value-of select="concat(dbk:personname/dbk:firstname, ' ', dbk:personname/dbk:surname)"/>
  </template>
  
  <template context="dbk:biblioentry//dbk:pubdate">
    <xsl:value-of select="concat('date={', ., '},&#xa;')"/>
  </template>
  
  <template context="dbk:biblioentry//dbk:publisher">
    <xsl:value-of select="concat('publisher={', dbk:publishername[1], '},&#xa;')"/>
  </template>
  
  <template context="dbk:biblioentry//dbk:pagenums">
    <xsl:value-of select="concat('pages={', ., '},&#xa;')"/>
  </template>
  
  <template context="dbk:biblioentry//dbk:volumenum">
    <xsl:value-of select="concat('number={', ., '},&#xa;')"/>
  </template>
  
  <template context="dbk:biblioentry//dbk:biblioset[not(following-sibling::dbk:biblioset[@relation = 'inproceedings'])]/dbk:title">
    <xsl:value-of select="concat('title={', ., '},&#xa;')"/>
  </template>

  <template context="dbk:biblioentry/dbk:biblioset[following-sibling::dbk:biblioset[@relation = 'inproceedings']]/dbk:title">
    <xsl:value-of select="concat('booktitle={', ., '},&#xa;')"/>
  </template>

  <template context="dbk:biblioentry//dbk:biblioid">
    <xsl:value-of select="concat(@class, '={', ., '},&#xa;')"/>
  </template>
  
  <template context="dbk:orderedlist">
    <xsl:variable name="list-type" as="xs:string?"
                  select="tr:enumerate-list-type((@numeration, 'arabic')[1], *:listitem[1]/@override)"/>
    <!-- use 1st override or if empty 1st override of sublist -->
    <xsl:variable name="override" as="xs:string?"
                  select="(*:listitem[1], 
                           *:listitem[1]/*:orderedlist[1]/*:listitem[1])[string-length(@override) gt 0][1]/@override"/>
    <xsl:variable name="start" as="xs:integer" 
                  select="if(string-length($override) gt 0 and @numeration) 
                          then tr:list-number-to-integer($override, @numeration) - 1
                          else 0"/>
    <xsl:variable name="level" select="count(ancestor::*:orderedlist|ancestor::*:itemizedlist) + 1" as="xs:integer"/>
    <xsl:variable name="level-roman" as="xs:string">
      <xsl:number value="$level" format="i"/>
    </xsl:variable>
    <xsl:value-of select="concat('&#xa;\begin{enumerate}',
                                 $list-type[not(matches(., '^\[\{\d+\.\}\]$'))],
                                 concat('&#xa;\setcounter{enum', 
                                        $level-roman, 
                                        '}{', 
                                        $start, 
                                        '}')[$start gt 0]
                                 )"/>
    <xsl:apply-templates mode="#current"/>
    <xsl:value-of select="'&#xa;\end{enumerate}&#xa;'"/>
  </template>

  <xsl:function name="tr:enumerate-list-type" as="xs:string">
    <xsl:param name="numeration" as="xs:string"/>
    <xsl:param name="override" as="xs:string?"/>
    <xsl:variable name="indizes"    select="tokenize($override, '\p{P}')[. ne '']"   as="xs:string*"/>
    <xsl:variable name="separators" select="tokenize($override, '[\w\d]+')[. ne '']" as="xs:string*"/>
    <xsl:variable name="list-type" as="xs:string"
                  select="     if($numeration eq 'loweralpha') then 'a'
                          else if($numeration eq 'upperalpha') then 'A'
                          else if($numeration eq 'lowerroman') then 'i'
                          else if($numeration eq 'upperroman') then 'I'
                          else                                      '1'"/>
    <xsl:value-of select="string-join(('[{',
                                      for $i in (1 to max((count($indizes), count($separators))))
                                      return if(    count($separators) gt count($indizes))
                                             then ($separators[$i], $list-type[$indizes[$i]])
                                             else ($list-type, $separators[$i]),
                                      '}]'         
                                      ), '')"/>
  </xsl:function>
  
  <xsl:function name="tr:list-number-to-integer" as="xs:integer">
    <xsl:param name="override" as="xs:string"/>
    <xsl:param name="list-type" as="xs:string"/>
    <xsl:variable name="counter" as="xs:string"
                  select="replace($override, '.*?([A-Z0-9]+).*', '$1', 'i')"/>
    <xsl:choose>
      <xsl:when test="$list-type = ('upperroman', 'lowerroman') and matches($counter, '[ivxlcdm]', 'i')">
        <xsl:value-of select="tr:roman-to-int($counter)"/>
      </xsl:when>
      <xsl:when test="$list-type = 'loweralpha' or matches($counter, '[a-z]')">
        <xsl:value-of select="string-length(substring-before('abcdefghijklmnopqrstuvwxyz', $counter)) + 1"/>
      </xsl:when>
      <xsl:when test="$list-type = 'upperalpha' or matches($counter, '[A-Z]')">
        <xsl:value-of select="string-length(substring-before('ABCDEFGHIJKLMNOPQRSTUVWXYZ', $counter)) + 1"/>
      </xsl:when>
      <xsl:when test="not($counter)">
        <xsl:value-of select="1"/>
      </xsl:when>
      <xsl:otherwise>
        <xsl:value-of select="xs:integer((normalize-space($counter)))"/>
      </xsl:otherwise>
    </xsl:choose>
  </xsl:function>

  <!-- ==================== Word layout fidelity (page geometry, SDT fields, sizes, spacing, header/footer) ==================== -->

  <xsl:variable name="docProps-sdt-roles" as="xs:string+"
                select="('Category', 'Title', 'Subject', 'Manager', 'Status', 'Company', 'Abstract',
                         'Keywords', 'Created', 'Modified', 'PrintDate', 'PublishDate', 'Author',
                         'LastModifiedBy', 'Revision', 'Comments', 'ContentType', 'Identifier')"/>

  <!-- SDT docProps wrappers (cover page fields such as Title, Subject, Status) are inline
       content, not quotations: neutralize the quote environment for them -->
  <template context="dbk:blockquote[@role = $docProps-sdt-roles]">
    <xsl:apply-templates select="node()" mode="#current"/>
  </template>

  <!-- font size on phrases (run level) -->
  <template context="dbk:phrase[@css:font-size]">
    <xsl:variable name="size" select="replace(@css:font-size, '[a-z]+$', '')" as="xs:string"/>
    <xsl:text>{\fontsize{</xsl:text>
    <xsl:value-of select="$size"/>
    <xsl:text>pt}{</xsl:text>
    <xsl:value-of select="round(number($size) * 1.15 * 10) div 10"/>
    <xsl:text>pt}\selectfont </xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>

  <!-- header/footer text keeps its Word font (e.g. Calibri for classification
       markings) when compiled with LuaLaTeX/XeLaTeX. The availability check must
       not run inside the output routine (a missing font triggers a luaotfload
       database reload there, which fails), so each font is resolved once in the
       preamble into a \docxhffont<n> macro. -->
  <xsl:variable name="docx-hf-fonts" as="xs:string*"
                select="distinct-values(//dbk:phrase/@css:font-family)"/>

  <!-- distinct Word letter-spacing values (pt, absolute) that get soul
       letterspacing (the &gt;=0.3pt filter mirrors the emission template) -->
  <xsl:variable name="docx-letterspacings" as="xs:string*"
                select="distinct-values(
                          (//dbk:phrase/@css:letter-spacing,
                           //css:rule/@css:letter-spacing)
                          [number(replace(., '[a-z]+$', '')) ge 0.3]
                          /replace(., '[a-z]+$', ''))"/>
  <template context="dbk:phrase[ancestor::dbk:div[@role = ('docx2hub:header', 'docx2hub:footer')]][@css:font-family]">
    <xsl:text>\ifPDFTeX\else\docxhffont</xsl:text>
    <xsl:value-of select="substring('ABCDEFGHIJKLMNOPQRSTUVWXYZ', index-of($docx-hf-fonts, @css:font-family), 1)"/>
    <xsl:text>\fi </xsl:text>
    <xsl:next-match/>
  </template>

  <!-- body runs keep their Word font (e.g. a Times New Roman print-size sample
       inside an Arial document); the macros resolve in the preamble and inherit
       the surrounding weight/shape because they are \newfontfamily switches -->
  <template context="dbk:phrase[@css:font-family][not(ancestor::dbk:div[@role = ('docx2hub:header', 'docx2hub:footer')])]">
    <xsl:text>\docxhffont</xsl:text>
    <xsl:value-of select="substring('ABCDEFGHIJKLMNOPQRSTUVWXYZ', index-of($docx-hf-fonts, @css:font-family), 1)"/>
    <xsl:text> </xsl:text>
    <xsl:next-match/>
  </template>

  <!-- Word PAGE / NUMPAGES field results (tagged by docx2hub) regenerate as live
       numbers, so they match the LaTeX pagination instead of Word's cached values -->
  <template context="dbk:phrase[@role eq 'hub:page-number']">
    <!-- {} ends the control word so the following space survives ("Page i of 6") -->
    <text>\thepage{}</text>
  </template>

  <template context="dbk:phrase[@role eq 'hub:page-count']">
    <text>\docxpagecount </text>
  </template>

  <!-- font size for the whole paragraph, including its \par (Word derives the
       line height from the tallest run; runs override the paragraph style).
       Only the paragraph's own runs count: phrases inside a nested floating
       textbox belong to that box's paragraphs, so an anchor paragraph holding
       only shapes must not wrap the whole body in the shapes' largest font.
       A pt-valued css:line-height (Word lineRule exact/atLeast) becomes the
       baselineskip, floored at the natural 1.15 x size leading (atLeast
       semantics; exact values below the natural leading are rare and would
       clip in Word). Inside a body v:textbox the wrapper ends with an explicit
       \par: the last paragraph of a box has no separator \par, and the
       implicit one at the parbox end would run after the group closed, taking
       the ambient font's leading instead of the paragraph's own. -->
  <template context="dbk:para[exists(.//dbk:phrase/@css:font-size)
                               or exists(key('style', @role)/@css:font-size)
                               or exists(@css:line-height[matches(., 'pt$')])]">
    <xsl:variable name="own-phrases" as="element(dbk:phrase)*"
                  select=".//dbk:phrase[ancestor::dbk:para[1] is current()]"/>
    <xsl:variable name="lh-pt" as="xs:double?"
                  select="number(replace(@css:line-height[matches(., 'pt$')], 'pt$', ''))[. = .]"/>
    <!-- header/footer boxes render in restricted horizontal mode, no \par there -->
    <xsl:variable name="in-textbox" as="xs:boolean"
                  select="exists(ancestor::v:textbox)
                          and not(ancestor::dbk:div[@role = ('docx2hub:header', 'docx2hub:footer')])"/>
    <xsl:variable name="eff-size" as="xs:double?"
                  select="(max(for $s in (if (exists($own-phrases/@css:font-size))
                                          then $own-phrases/@css:font-size
                                          else (@css:font-size, key('style', @role)/@css:font-size))
                                      return number(replace($s, '[a-z]+$', ''))[. = .]),
                          if ($in-textbox or exists($lh-pt)) then 12 else (),
                         ())[1]"/>
    <xsl:variable name="baseline" as="xs:double?"
                  select="if (empty($eff-size)) then ()
                          else if (exists($lh-pt))
                          then max(($lh-pt, round($eff-size * 1.15 * 10) div 10))
                          else round($eff-size * 1.15 * 10) div 10"/>
    <xsl:variable name="wrap" as="xs:boolean"
                  select="exists($eff-size) and ($eff-size ne 12 or $in-textbox or exists($lh-pt))"/>
    <xsl:if test="$wrap">
      <xsl:text>{\fontsize{</xsl:text>
      <xsl:value-of select="$eff-size"/>
      <xsl:text>pt}{</xsl:text>
      <xsl:value-of select="round($baseline * 10) div 10"/>
      <xsl:text>pt}\selectfont </xsl:text>
      <!-- a zero-height line whose baseline defines the parbox reference: the
           first text baseline then lands exactly at textblock y + line height,
           independent of the first line's font ascent (Word's rule, see the
           textbox template) -->
      <xsl:if test="$in-textbox
                    and not(preceding-sibling::dbk:para)
                    and normalize-space(string(.))">
        <xsl:text>\null\par </xsl:text>
      </xsl:if>
    </xsl:if>
    <xsl:next-match/>
    <xsl:if test="$wrap">
      <xsl:if test="$in-textbox">
        <!-- an empty paragraph is Word's spacer line: \null starts horizontal
             mode so the \par actually creates a line box at the paragraph's
             own line height (a bare \par in vertical mode is a no-op) -->
        <xsl:if test="not(normalize-space(string(.)))">
          <xsl:text>\null </xsl:text>
        </xsl:if>
        <xsl:text>\par</xsl:text>
      </xsl:if>
      <xsl:text>}</xsl:text>
    </xsl:if>
  </template>

  <!-- paragraph colour from a run style override or the paragraph style; \color
       declaration instead of \textcolor so it survives the paragraph break -->
  <template context="dbk:para[(@css:color, key('style', @role)/@css:color)[1]]
                             [not(every $i in tr:hex-rgb-color-to-ints((@css:color, key('style', @role)/@css:color)[1]) 
                                  satisfies $i &lt; 35)]
                             [not(@role = ('ZFinlineequation', 'ZFequation', 'ZFVerweis', 'ZFCaption', 'NOTE-CE', 'NOTE-TS'))]
                             [not(ancestor-or-self::dbk:link)]">
    <xsl:text>{\color{</xsl:text>
    <xsl:value-of select="for $color-code in (@css:color, key('style', @role)/@css:color)[1] 
                          return if (exists(tr:color-hex-rgb-to-keyword($color-code)))
                                 then tr:color-hex-rgb-to-keyword($color-code)[1]
                                 else concat('color-', upper-case(substring-after($color-code, '#')))"/>
    <xsl:text>} </xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>

  <!-- paragraph spacing: explicit margin-top -->
  <template context="dbk:para[@css:margin-top[matches(., '^[1-9]')]][not(@docx2tex:config eq 'headline')][preceding::dbk:para[@docx2hub:section-end eq 'true']][not(ancestor::dbk:listitem)]">
    <xsl:text>\vspace*{</xsl:text>
    <xsl:value-of select="replace(@css:margin-top, '[a-z]+$', '')"/>
    <xsl:text>pt}</xsl:text>
    <xsl:next-match/>
  </template>

  <!-- paragraph spacing: explicit margin-bottom -->
  <template context="dbk:para[@css:margin-bottom[matches(., '^[1-9]')]][not(@docx2tex:config eq 'headline')][preceding::dbk:para[@docx2hub:section-end eq 'true']][not(ancestor::dbk:listitem)]">
    <xsl:next-match/>
    <xsl:text>\vspace{</xsl:text>
    <xsl:value-of select="replace(@css:margin-bottom, '[a-z]+$', '')"/>
    <xsl:text>pt}</xsl:text>
  </template>

  <!-- paragraph spacing: margin-top from paragraph style -->
  <template context="dbk:para[key('style', @role)/@css:margin-top[matches(., '^[1-9]')]][not(@css:margin-top)]
                             [not(@docx2tex:config eq 'headline')][preceding::dbk:para[@docx2hub:section-end eq 'true']][not(ancestor::dbk:listitem)]">
    <xsl:text>\vspace*{</xsl:text>
    <xsl:value-of select="replace(key('style', @role)/@css:margin-top, '[a-z]+$', '')"/>
    <xsl:text>pt}</xsl:text>
    <xsl:next-match/>
  </template>

  <!-- Word keeps the space between two paragraphs at max(space-after of the first,
       space-before of the second); LaTeX sums the explicit \vspace and \parskip.
       For heading paragraphs (whose Word margins are explicit), fold the spacing
       accordingly: the paragraph break already contributes \parskip (8.5pt, the
       pipeline's Word-Normal approximation), so only the excess is emitted, and a
       heading's space-before is reduced by the space-after it actually follows. -->
  <template context="dbk:para[@docx2tex:config eq 'headline'][@css:margin-bottom[matches(., '^[1-9]')]]">
    <xsl:next-match/>
    <xsl:variable name="after" select="number(replace(@css:margin-bottom, '[a-z]+$', ''))" as="xs:double"/>
    <xsl:variable name="excess" select="max((0, $after - 8.5))" as="xs:double"/>
    <xsl:if test="$excess gt 0">
      <xsl:text>\vspace{</xsl:text>
      <xsl:value-of select="round($excess * 10) div 10"/>
      <xsl:text>pt}</xsl:text>
    </xsl:if>
  </template>

  <template context="dbk:para[@docx2tex:config eq 'headline'][@css:margin-top[matches(., '^[1-9]')]]">
    <xsl:call-template name="docx2tex-headline-margin-top">
      <xsl:with-param name="top" select="number(replace(@css:margin-top, '[a-z]+$', ''))"/>
    </xsl:call-template>
  </template>

  <template context="dbk:para[@docx2tex:config eq 'headline'][key('style', @role)/@css:margin-top[matches(., '^[1-9]')]][not(@css:margin-top)]">
    <xsl:call-template name="docx2tex-headline-margin-top">
      <xsl:with-param name="top" select="number(replace(key('style', @role)/@css:margin-top, '[a-z]+$', ''))"/>
    </xsl:call-template>
  </template>

  <xsl:template name="docx2tex-headline-margin-top">
    <xsl:param name="top" as="xs:double"/>
    <!-- the preceding paragraph's effective bottom glue: its space-after, but at
         least \parskip (which the paragraph break always inserts) -->
    <xsl:variable name="prev-bottom" as="xs:double"
                  select="(number(replace(preceding-sibling::dbk:para[1]/@css:margin-bottom, '[a-z]+$', ''))[. = .], 0)[1]"/>
    <xsl:variable name="excess" select="max((0, $top - max(($prev-bottom, 8.5))))" as="xs:double"/>
    <xsl:if test="$excess gt 0">
      <xsl:text>\vspace*{</xsl:text>
      <xsl:value-of select="round($excess * 10) div 10"/>
      <xsl:text>pt}</xsl:text>
    </xsl:if>
    <xsl:next-match/>
  </xsl:template>

  <!-- Cover paragraphs (before the first Word section break): Word stacks line
       boxes, LaTeX adds the previous line's depth and the next line's ascent, so the
       raw space-after under/overshoots after large fonts. The cover emission
       compensates: glue = space-after (Word's max rule; \parskip 8.5pt already
       contributed by the paragraph break) + 0.775 x (own line height - next
       paragraph's line height), and a page-top space-before sheds LaTeX's topskip
       and ascent excess (Word starts big lines ~6pt below the box top). -->
  <xsl:template name="docx2tex-cover-size" as="xs:double">
    <xsl:sequence select="max((for $sz in (.//dbk:phrase/@css:font-size, @css:font-size, key('style', @role)/@css:font-size)
                                 return number(replace($sz, '[a-z]+$', ''))[. = .], 12))[1]"/>
  </xsl:template>

  <template context="dbk:para[not(preceding::dbk:para[@docx2hub:section-end eq 'true'])]
                             [@css:margin-bottom[matches(., '^[1-9]')]]
                             [not(ancestor::dbk:listitem)]">
    <xsl:next-match/>
    <xsl:variable name="after" select="number(replace(@css:margin-bottom, '[a-z]+$', ''))" as="xs:double"/>
    <xsl:variable name="own-size" as="xs:double">
      <xsl:for-each select=".">
        <xsl:call-template name="docx2tex-cover-size"/>
      </xsl:for-each>
    </xsl:variable>
    <xsl:variable name="next-size" as="xs:double">
      <xsl:for-each select="following-sibling::dbk:para[1]">
        <xsl:call-template name="docx2tex-cover-size"/>
      </xsl:for-each>
    </xsl:variable>
    <xsl:variable name="next-size" select="if (empty(following-sibling::dbk:para[1])) then 12 else ($next-size, 12)[1]" as="xs:double"/>
    <xsl:variable name="glue" select="$after - 8.5 + 0.775 * 1.15 * ($own-size - $next-size)" as="xs:double"/>
    <xsl:if test="abs($glue) gt 0.05">
      <xsl:text>\vspace{</xsl:text>
      <xsl:value-of select="round($glue * 10) div 10"/>
      <xsl:text>pt}</xsl:text>
    </xsl:if>
  </template>

  <template context="dbk:para[not(preceding::dbk:para[@docx2hub:section-end eq 'true'])]
                             [key('style', @role)/@css:margin-bottom[matches(., '^[0-9]')]]
                             [not(@css:margin-bottom)]
                             [not(ancestor::dbk:listitem)]">
    <xsl:next-match/>
    <xsl:variable name="after" select="number(replace(key('style', @role)/@css:margin-bottom, '[a-z]+$', ''))" as="xs:double"/>
    <xsl:variable name="own-size" as="xs:double">
      <xsl:for-each select=".">
        <xsl:call-template name="docx2tex-cover-size"/>
      </xsl:for-each>
    </xsl:variable>
    <xsl:variable name="next-size" as="xs:double">
      <xsl:for-each select="following-sibling::dbk:para[1]">
        <xsl:call-template name="docx2tex-cover-size"/>
      </xsl:for-each>
    </xsl:variable>
    <xsl:variable name="next-size" select="if (empty(following-sibling::dbk:para[1])) then 12 else ($next-size, 12)[1]" as="xs:double"/>
    <xsl:variable name="glue" select="$after - 8.5 + 0.775 * 1.15 * ($own-size - $next-size)" as="xs:double"/>
    <xsl:if test="abs($glue) gt 0.05">
      <xsl:text>\vspace{</xsl:text>
      <xsl:value-of select="round($glue * 10) div 10"/>
      <xsl:text>pt}</xsl:text>
    </xsl:if>
  </template>

  <template context="dbk:para[not(preceding::dbk:para[@docx2hub:section-end eq 'true'])]
                             [@css:margin-top[matches(., '^[1-9]')]]
                             [not(ancestor::dbk:listitem)]">
    <xsl:variable name="before" select="number(replace(@css:margin-top, '[a-z]+$', ''))" as="xs:double"/>
    <xsl:variable name="own-size" as="xs:double">
      <xsl:for-each select=".">
        <xsl:call-template name="docx2tex-cover-size"/>
      </xsl:for-each>
    </xsl:variable>
    <xsl:text>\vspace*{</xsl:text>
    <xsl:value-of select="round(($before - 0.775 * 1.15 * $own-size - 6) * 10) div 10"/>
    <xsl:text>pt}</xsl:text>
    <xsl:next-match/>
  </template>

  <!-- line height (multiple line spacing, e.g. Word 1.08 lines) -->
  <template context="dbk:para[@css:line-height[matches(., '^\d\.')][not(. eq '1')]]">
    <xsl:text>{\linespread{</xsl:text>
    <xsl:value-of select="round(number(@css:line-height) * 100) div 100"/>
    <xsl:text>}\selectfont </xsl:text>
    <xsl:next-match/>
    <xsl:text>}</xsl:text>
  </template>

  <!-- manual page breaks (not Word section boundaries) -->
  <template context="dbk:para[@css:page-break-after eq 'always'][not(@docx2hub:section-end eq 'true')][not(parent::dbk:listitem)]">
    <xsl:next-match/>
    <xsl:text>\clearpage&#xa;</xsl:text>
  </template>

  <!-- Word section breaks: new page, the next section's page style and page-number
       format/restart (surfaced by docx2hub as css:page-number-* on this para) -->
  <template context="dbk:para[@css:page-break-after eq 'always'][@docx2hub:section-end eq 'true'][not(parent::dbk:listitem)]">
    <xsl:variable name="break-index" select="count(preceding::dbk:para[@docx2hub:section-end eq 'true']) + 1" as="xs:integer"/>
    <xsl:variable name="section" select="$break-index + 1" as="xs:integer"/>
    <xsl:variable name="docx-headers" select="//dbk:div[@role eq 'docx2hub:header'][@condition eq 'default']" as="element(dbk:div)*"/>
    <xsl:variable name="docx-footers" select="//dbk:div[@role eq 'docx2hub:footer'][@condition eq 'default']" as="element(dbk:div)*"/>
    <xsl:if test="$break-index eq 1">
      <!-- end the cover's footer-band reservation BEFORE the page break: the page-1
           shipout refreshes the page-builder column height from \textheight, so a
           later restore would leave page 2 with the reduced height (footer riding
           high). Page 1 itself was already built against the reduced height. -->
      <xsl:if test="$cover-footer-reserve">
        <xsl:text>\addtolength{\textheight}{</xsl:text>
        <xsl:value-of select="replace($cover-footer-reserve, '-', '')"/>
        <xsl:text>}&#xa;</xsl:text>
      </xsl:if>
    </xsl:if>
    <xsl:text>\clearpage&#xa;</xsl:text>
    <xsl:if test="$break-index eq 1">
      <!-- the cover artwork (logo, vision band) ends with the cover section; the
           classification marking applies to every page and is re-added -->
      <xsl:text>\ClearShipoutPicture&#xa;\AddToShipoutPicture{\docxmarking}&#xa;</xsl:text>
    </xsl:if>
    <xsl:if test="@css:page-number-format or @css:page-number-start">
      <xsl:text>\pagenumbering{</xsl:text>
      <xsl:value-of select="if (@css:page-number-format eq 'lowerRoman') then 'roman'
                            else if (@css:page-number-format eq 'upperRoman') then 'Roman'
                            else if (@css:page-number-format eq 'lowerLetter') then 'alph'
                            else if (@css:page-number-format eq 'upperLetter') then 'Alph'
                            else 'arabic'"/>
      <xsl:text>}&#xa;</xsl:text>
      <xsl:if test="@css:page-number-start[. castable as xs:integer][xs:integer(.) ne 1]">
        <xsl:text>\setcounter{page}{</xsl:text>
        <xsl:value-of select="xs:integer(@css:page-number-start)"/>
        <xsl:text>}&#xa;</xsl:text>
      </xsl:if>
    </xsl:if>
    <!-- the section's own page margins (Word w:pgMar, e.g. a tall top margin on a
         closing page): \newgeometry applies them from this page on; the header
         rule keeps its absolute position (62.4pt below the page top, as measured
         in the reference), so the head distance follows the new top margin -->
    <xsl:if test="@css:page-margin-top and @css:page-margin-bottom">
      <xsl:variable name="sec-top" select="number(replace(@css:page-margin-top, '[a-z]+$', ''))" as="xs:double"/>
      <!-- landing-style closing pages (huge top margin) render in the reference
           with the section's header distance added below the margin -->
      <xsl:variable name="sec-top-adjusted" as="xs:double"
                    select="if ($sec-top ge 300 and @css:page-header-distance)
                            then $sec-top + number(replace(@css:page-header-distance, '[a-z]+$', ''))
                            else $sec-top"/>
      <!-- header text baseline ~43.5pt below the page top; the rule stays at
           ~62.4pt via \headruleskip -->
      <xsl:variable name="sec-left" select="replace((@css:page-margin-left, '56.7pt')[1], '[a-z]+$', '')" as="xs:string"/>
      <xsl:variable name="sec-right" select="replace((@css:page-margin-right, '56.7pt')[1], '[a-z]+$', '')" as="xs:string"/>
      <xsl:text>\newgeometry{top=</xsl:text>
      <xsl:value-of select="round($sec-top-adjusted * 10) div 10"/>
      <xsl:text>pt,bottom=</xsl:text>
      <xsl:value-of select="replace(@css:page-margin-bottom, '[a-z]+$', '')"/>
      <xsl:text>pt,left=</xsl:text>
      <xsl:value-of select="$sec-left"/>
      <xsl:text>pt,right=</xsl:text>
      <xsl:value-of select="$sec-right"/>
      <xsl:text>pt,headheight=14pt,headsep=</xsl:text>
      <xsl:value-of select="round(($sec-top-adjusted - 47.5) * 10) div 10"/>
      <xsl:text>pt,footskip=20pt}&#xa;</xsl:text>
    </xsl:if>
    <xsl:if test="exists($docx-headers[$section])">
      <xsl:text>\pagestyle{docx-section-</xsl:text>
      <xsl:value-of select="$section"/>
      <xsl:text>}\renewcommand{\chapterpagestyle}{docx-section-</xsl:text>
      <xsl:value-of select="$section"/>
      <xsl:text>}&#xa;</xsl:text>
    </xsl:if>
    <!-- the new section's absolutely positioned footer art (e.g. a full-bleed band),
         in addition to the shared marking -->
    <xsl:for-each select="$docx-footers[$section]">
      <xsl:variable name="band-images" as="element(dbk:imagedata)*"
                    select=".//dbk:imagedata[@fileref][@css:width][ancestor::dbk:div[@role eq 'docx2hub:footer']]"/>
      <xsl:if test="exists($band-images)">
        <xsl:text>\AddToShipoutPicture{%&#xa;</xsl:text>
        <xsl:apply-templates select="$band-images" mode="#current"/>
        <xsl:text>}&#xa;</xsl:text>
      </xsl:if>
    </xsl:for-each>
  </template>

  <!-- Word tables flow inline where they appear, they are not floats -->
  <template context="dbk:informaltable[not(parent::dbk:entry)]">
    <xsl:text>\noindent </xsl:text>
    <xsl:apply-templates select="node()" mode="#current"/>
    <xsl:text>&#xa;</xsl:text>
  </template>

  <!-- drop the docx2hub header/footer specs from the body flow;
       their content is rendered in the preamble via fancyhdr -->
  <template context="dbk:div[@role = ('docx2hub:header-spec', 'docx2hub:footer-spec')]"/>

  <!-- reserve the footer band on the pages before the first section break (the cover):
       Word pushes body text above a tall footer; the LaTeX geometry does not know about
       the absolutely positioned band. Undone at the first section break below. -->
  <template context="/dbk:hub/dbk:div[@role = ('docx2hub:header-spec', 'docx2hub:footer-spec')][1]">
    <xsl:if test="$cover-footer-reserve">
      <xsl:text>\addtolength{\textheight}{</xsl:text>
      <xsl:value-of select="$cover-footer-reserve"/>
      <xsl:text>}&#xa;</xsl:text>
    </xsl:if>
    <xsl:next-match/>
  </template>

  <!-- paragraphs inside header/footer only emit inline text content (no paragraph
       breaks, no images: those are placed separately at absolute positions) -->
  <template context="dbk:para[ancestor::dbk:div[@role = ('docx2hub:header', 'docx2hub:footer')]]">
    <xsl:variable name="hf-size" as="xs:double?"
                  select="max(for $sz in (if (exists(.//dbk:phrase/@css:font-size))
                                           then .//dbk:phrase/@css:font-size
                                           else (@css:font-size, key('style', @role)/@css:font-size))
                                       return number(replace($sz, '[a-z]+$', '')))"/>
    <xsl:variable name="hf-bold" as="xs:boolean"
                  select="string((@css:font-weight, key('style', @role)/@css:font-weight)[1]) eq 'bold'"/>
    <xsl:if test="exists($hf-size) and $hf-size ne 12">
      <xsl:text>{\fontsize{</xsl:text>
      <xsl:value-of select="$hf-size"/>
      <xsl:text>pt}{</xsl:text>
      <xsl:value-of select="round($hf-size * 1.15 * 10) div 10"/>
      <xsl:text>pt}\selectfont </xsl:text>
    </xsl:if>
    <xsl:if test="$hf-bold">
      <xsl:text>\bfseries </xsl:text>
    </xsl:if>
    <xsl:apply-templates select="node()[not(self::dbk:mediaobject or self::dbk:inlinemediaobject or self::dbk:br)]" mode="#current"/>
    <xsl:if test="exists($hf-size) and $hf-size ne 12">
      <xsl:text>}</xsl:text>
    </xsl:if>
  </template>

  <!-- line breaks have no meaning inside absolutely positioned header/footer text -->
  <template context="dbk:br[ancestor::dbk:div[@role = ('docx2hub:header', 'docx2hub:footer')]]"/>

  <!-- Word srcRect/VML crops as graphicx trim: crop fractions are relative to the
       image's natural size, which only TeX knows, so measure it first with \sbox -->
  <xsl:function name="tr:crop-includegraphics" as="xs:string">
    <xsl:param name="img" as="element(dbk:imagedata)"/>
    <xsl:variable name="l" as="xs:double" select="number(($img/@css:crop-left ! substring-before(., '%'), '0')[1]) div 100"/>
    <xsl:variable name="t" as="xs:double" select="number(($img/@css:crop-top ! substring-before(., '%'), '0')[1]) div 100"/>
    <xsl:variable name="r" as="xs:double" select="number(($img/@css:crop-right ! substring-before(., '%'), '0')[1]) div 100"/>
    <xsl:variable name="b" as="xs:double" select="number(($img/@css:crop-bottom ! substring-before(., '%'), '0')[1]) div 100"/>
    <xsl:sequence select="if ($l eq 0 and $t eq 0 and $r eq 0 and $b eq 0)
                          then '\includegraphics['
                          else concat('\sbox\docxcroppedimagebox{\includegraphics{', replace($img/@fileref, '([%#])', '\\$1'), '}}%', '&#xa;',
                                      '\includegraphics[trim={\dimexpr', $l, '\wd\docxcroppedimagebox\relax} {\dimexpr', $b, '\ht\docxcroppedimagebox\relax} {\dimexpr', $r, '\wd\docxcroppedimagebox\relax} {\dimexpr', $t, '\ht\docxcroppedimagebox\relax}, clip, ')"/>
  </xsl:function>

  <!-- logo in the Word header: left margin minus the Word anchor offset (-9.75pt),
       top edge 33.25pt below the page top, at its Word size -->
  <template context="dbk:imagedata[@fileref][@css:width][ancestor::dbk:div[@role eq 'docx2hub:header']]">
    <xsl:text>  \put(46.95,</xsl:text>
    <xsl:value-of select="number(replace(/dbk:hub/@css:page-height, '[a-z]+$', '')) - 33.25 - number(replace(@css:height, '[a-z]+$', ''))"/>
    <xsl:text>){</xsl:text>
    <xsl:value-of select="tr:crop-includegraphics(.)"/>
    <xsl:text>width=</xsl:text>
    <xsl:value-of select="replace(@css:width, '[a-z]+$', '')"/>
    <xsl:text>pt,height=</xsl:text>
    <xsl:value-of select="replace(@css:height, '[a-z]+$', '')"/>
    <xsl:text>pt]{</xsl:text>
    <xsl:value-of select="replace(@fileref, '([%#])', '\\$1')"/>
    <xsl:text>}}&#xa;</xsl:text>
  </template>

  <!-- banner image in the Word footer: flush with the page bottom and left edge,
       spanning the full page width at its Word size -->
  <template context="dbk:imagedata[@fileref][@css:width][ancestor::dbk:div[@role eq 'docx2hub:footer']]">
    <xsl:text>  \put(0,0){</xsl:text>
    <xsl:value-of select="tr:crop-includegraphics(.)"/>
    <xsl:text>width=\paperwidth,height=</xsl:text>
    <xsl:value-of select="replace(@css:height, '[a-z]+$', '')"/>
    <xsl:text>pt]{</xsl:text>
    <xsl:value-of select="replace(@fileref, '([%#])', '\\$1')"/>
    <xsl:text>}}&#xa;</xsl:text>
  </template>

  <!-- vector shapes (rendered as SVG by docx2hub) in the Word header become thin
       separator rules, centered on the page, top edge 94pt below the page top -->
  <template context="dbk:imagedata[svg:svg][ancestor::dbk:div[@role = ('docx2hub:header', 'docx2hub:footer')]]">
    <xsl:variable name="w" select="replace(svg:svg/@width, '[a-z]+$', '')" as="xs:string"/>
    <xsl:text>  \put(</xsl:text>
    <xsl:value-of select="round((number(replace(/dbk:hub/@css:page-width, '[a-z]+$', '')) - number($w)) div 2 * 100) div 100"/>
    <xsl:text>,</xsl:text>
    <xsl:value-of select="number(replace(/dbk:hub/@css:page-height, '[a-z]+$', '')) - 95"/>
    <xsl:text>){\rule{</xsl:text>
    <xsl:value-of select="$w"/>
    <xsl:text>pt}{1pt}}&#xa;</xsl:text>
  </template>

  <!-- Word minimum row height: keep single-line cells at the entry's min-height -->
  <template context="dbk:para[parent::dbk:entry[1][@css:min-height][not(dbk:para[2])]]
                             [not(preceding-sibling::dbk:para)]
                             [number(replace(parent::dbk:entry/@css:min-height, '[a-z]+$', '')) gt 0]">
    <xsl:text>\rule{0pt}{</xsl:text>
    <xsl:value-of select="replace(parent::dbk:entry/@css:min-height, '[a-z]+$', '')"/>
    <xsl:text>pt}</xsl:text>
    <xsl:next-match/>
  </template>

</set>
