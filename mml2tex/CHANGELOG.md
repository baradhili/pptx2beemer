# Changelog — mml2tex fork, `front-page-layout`

The fork tracks upstream transpect/mml2tex; no fork-specific commits. Pinned at
the upstream commit the pipeline requires (bold Greek via \boldsymbol handling).
